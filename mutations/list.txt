# SPDX-FileCopyrightText: 2026 Mete Balci
# SPDX-License-Identifier: AGPL-3.0-or-later
#
# The mutation list: bugs the checks have to catch.
#
# Every record below breaks the fabric in one specific way.  `make mutants`
# applies each to a copy of rtl/ and tb/, rebuilds, and runs the check that is
# supposed to notice.  A mutation the check catches is a check doing its job.
# One that survives is a hole, and it stays in the list until the check is
# fixed --- it is never quietly dropped.
#
# WHERE THE LIST CAME FROM.  CLAUDE.md's "What went wrong, and what caught it"
# records eight bugs, every one of which passed something before it was found.
# Those are the seed, because a check that would not catch a bug that was
# really made is not a check.  In order, and by the name each has here:
#
#   -MEMACK on a write is combinational
#       memack-registered-on-a-write
#   a request standing at the master clock edge is granted at that edge
#       grant-waits-for-a-later-edge
#   the timeout oscillator free-runs from power-on
#       timeout-oscillator-restarted-at-the-grant
#   a shadow memory must come from the stimulus, never the DUT
#       bridge-writes-the-address-instead-of-the-data
#       bridge-drops-an-address-bit
#   reads and writes must be able to meet
#       NOT HERE.  That one is in the stimulus generator rather than in the
#       fabric --- golden/src/busint_xbus.rs asserts the coprimality --- and a
#       mutation of it needs golden/ copied and rebuilt per mutation.  Issue
#       #6.  So the claim holds for seven of the eight, not eight.
#   a slave that only records the address cannot see a duplicate handshake
#       awvalid-held-up-after-awready
#   a stimulus more polite than the real consumer tests less
#       master-returns-to-idle-early
#   mutation runners must not hide build failures
#       NOT A MUTATION.  It is a property of the runner, and run.py holds it:
#       nothing reuses a build directory, and a build that fails is reported
#       as BROKEN and exits non-zero rather than counting as anything.
#
# The rest of the list is not a regression suite for those.  It walks the
# constants, the boundaries and the handshakes of each check in turn, because
# the question is whether the check covers its module, not whether it covers
# eight remembered afternoons.
#
# LINT MUST NOT DO THE CATCHING.  Verilator runs -Wall with warnings fatal, so
# a mutation that leaves a signal or even a *bit* of a signal unread fails to
# build and the check never sees it.  That is why several records below are
# written to keep every bit used --- the address mixed into the word rather
# than replacing it, a constant moved rather than a term deleted.  Where the
# obvious way to write a bug would be rejected by lint, the note says so.
#
# THE FORMAT.  Records are literal text, not patches: no line numbers and no
# context, so a record rots only when the lines it actually names change.
# `@old` must match its file exactly once or the run fails.  Every line
# between `@old`, `@new` and `@end` is source, verbatim, including its
# leading whitespace.
#
#   @mutation  a kebab-case name; how a survivor is reported
#   @check     which check has to catch it; `run.py`'s CHECKS is the list
#   @file      which file to mutate; the check must build it
#   @hole      optional: the issue holding a known hole open, as `@hole #3`
#   @note      prose, repeatable; printed when the mutation survives
#   @old       the source to replace, exactly as it stands
#   @new       what to put there; empty deletes
#   @end
#
# A slice gets mutations when it lands, and not before.  `microcycle` was
# deliberately absent while stage 4 was being written, on the grounds that a
# record against a file still moving rots before it is read; it has records
# now because the slices landed.  The same will be true of whatever comes
# after it.
#
# WHAT IS KNOWINGLY NOT CAUGHT.  Some records below carry `@hole`, which names
# the issue that holds the hole open.  They are reported every run, counted in
# their own column, and do not fail it.  How many there are is the run's answer
# and is deliberately not written here: a count in prose is a thing that goes
# stale the first time somebody closes one.
#
# A CLOSED ISSUE MEANS THE RECORD NEEDS REVISITING, and nothing here can
# tell you that. The inverse check fires when a hole *closes* --- the
# mutation becomes caught and the run fails, naming the issue and the line.
# It cannot fire when the ISSUE closes while the mutation still survives,
# which is what happens when a survivor turns out to be an equivalence: the
# question is answered, the `@hole` is now a suppression of nothing, and the
# run stays green. It happened once, to #5. So when an issue named here is
# closed, come back to its record: either the mutation is caught now, or it
# never could be and belongs rewritten with the reasoning, as #5's is.
#
# NOT EVERY `@hole` IS A DEBT. Some are predictions, and one of them has
# already come true. #9's two records were held open by the composed check
# stopping at a wall --- about five hundred bus cycles at the very end of the
# boot PROM, too few reads for the mutation to reach a compared column --- and
# the note said they should begin failing ON THEIR OWN when that wall moved.
# It moved; the run reported CLOSED, named the issue and the line, and the
# records are live now with nothing changed in them. That is the argument for
# the inverse check in one paragraph: a comment saying "revisit when X" trusts
# somebody to remember, and this enforced the revisit instead.
#
# #1's two records are the same kind of prediction and are still open: two of
# the three terms of -WAIT that no observed program fires. They are not
# defects, and the day a program exercises the stall path they will say so.
#
# `@hole` is not a way to make a survivor go away, and three things stop it
# from becoming one.
#
# It takes an issue number, so a hole cannot be recorded without being
# written down somewhere a person will see it.
#
# The record stays in the list, mutating exactly what it did before.  A
# survivor that is quietly deleted is a check that silently got weaker, and
# nothing afterwards would show it.
#
# And a mutation that is CAUGHT while still carrying an `@hole` fails the run
# --- the hole has closed and the record has not caught up.  That is the
# inverse check, and it is the part that keeps the field safe to have: without
# it the list would accumulate `@hole`s for holes that shut long ago.
#
# The reason to have it at all is that a target red by design cannot report a
# fifth survivor: red for four known reasons looks exactly like red for five.
# A recorded exception with a reason is this repository's existing idiom ---
# CLAUDE.md's "Where the fabric parts from muir", and the testbench not
# comparing -TPR60 while RESET is high. An unrecorded one is what nothing here
# tolerates.

#
# ------------------------------------------------------------------------
# HOW A RECORD GOES STALE, AND IN WHICH DIRECTION
#
# The literal text rots LOUDLY and the reasoning rots SILENTLY.  That
# asymmetry is the whole of what to watch.  Four directions, three noisy:
#
#   THE `@old` STOPS MATCHING.  The file moved under the record --- a rename,
#   a reindent, a line that grew a comment.  UNAPPLIED, and the run fails
#   naming the record and the file.
#
#   THE `@old` MATCHES TWICE.  A line was duplicated, and a record that could
#   mutate either one tests whichever the parser reached first.  Exactly once
#   or the run fails; that rule exists for this.
#
#   THE `@new` NAMES SOMETHING THAT HAS GONE.  The mutant does not build:
#   BROKEN, which is never a verdict on the check and always one on the
#   record.
#
#   AND THE SILENT ONE --- A WHOLE CHECK, OR A WHOLE FILE, WITH NOTHING AIMED
#   AT IT.  Nothing about this is ever wrong.  The run is green, every count
#   is right, and a module is untested.  Two guards exist because nothing else
#   would ever say so: `check_coverage` names a file a check builds that no
#   record touches, and `check_makefile` reads the Makefile in BOTH
#   directions --- "the Makefile runs a check this runner has never heard of"
#   being the direction that hides.  `machine` was exactly that for a while.
#
# The `@note`s are prose and nothing checks them.  When a note names a number,
# a microcycle, a file or an issue, that is the part of the record most likely
# to be false, and it fails no run when it is.  #5's `@hole` outlived the
# issue that justified it and every run stayed green.
#
# ------------------------------------------------------------------------
# WHEN A MUTATION SURVIVES: SIX THAT COULD NOT HAVE DONE ANYTHING ELSE
#
# A survivor is a finding, never something to tune away.  But the first
# question is not "which check is weak".  Six records here document mutations
# nothing can catch, and every one has the same shape: THE MUTATION PRESERVED
# AN INVARIANT THE CHECK DEPENDS ON.  Not a gap in the check --- something
# upstream holding still, which makes the change unobservable.
#
#   the timer's edge test, dropped      a rise not counted puts the next rise
#                                       a full period on: the same instant
#                                       the fall in between would give
#   the bridge latching on a write      the gate that stops a write strobing
#     (filed as hole #5 first)          MD is the processor's, not the
#                                       bridge's
#   the scratchpad compare, all ten     `wadr[9:5]` is zero on every cycle
#     bits instead of five              `destmd` is set
#   registering the map's first level   its address is a microcycle register,
#                                       so it is constant for the cycle
#   crossing -MEMACK and -LOADMD        `cadr_busint_xbus.sv` assigns the same
#                                       expression to both
#   rotating the device write word      all 5,650 device writes the boot PROM
#                                       makes carry zero, and a bijection on
#                                       zero is zero
#
# So when something survives, ask WHAT IS CONSTANT HERE THAT I DID NOT NOTICE.
# And notice which kind of constant it is: three of the six are constants of
# the WORKLOAD and not of the design --- the all-zero device word, the boot
# PROM's traffic, an address that only holds still because both its sources
# are microcycle registers.  Those stop being true the day the workload
# changes, so the device-word record's check counts distinct words and says so
# when the count rises above one.  The note goes wrong on its own and the run
# announces it, rather than waiting for somebody to remember.
#
# SWEEP THE MAGNITUDE; DO NOT ASK A YES/NO QUESTION.  Delaying the map by one
# tick survives, and so do 4 and 16; 28 is caught on PCS0 at microcycle
# 2,084,731 and 44 on JCOND at 1,062,763.  The answer worth having was not
# "caught" but WHERE THE EDGE IS --- the check is blind only below one
# microcycle, which is exactly where the staleness is not real.  A yes/no
# question would have recorded an equivalence and stopped there.
#
# Hence the rule: REPLACE AN EQUIVALENT MUTANT WITH THE SMALLEST MAGNITUDE
# THAT IS CAUGHT.  An equivalent mutant must be explained forever and tests
# nothing; a live one testing the same property costs the same line.
#
# ------------------------------------------------------------------------
# A CHECK THAT GOT WEAKER, AND `--since`
#
# A survivor and a regression look identical: nothing caught it, and the list
# either says why or does not.  `mutations/run.py --since <rev>` re-runs
# everything NOT CAUGHT NOW against that revision, archived, and reports which
# were caught there --- so "this used to be caught" is something the run
# states rather than something a person happens to notice.
#
# IT COVERS `@hole` RECORDS AS WELL AS SURVIVORS, and that is the point rather
# than an extra.  A recorded exception is exactly where a regression hides:
# the run is green, the record says "known", and nothing asks whether it was
# always known.  The one regression found this way, #12, was carried by a
# mutation that happened to have no `@hole` on it.  That was luck.  With one,
# the run would have stayed green and been confident about it.
#
# ------------------------------------------------------------------------
# THE PATH THAT ONLY EXISTS ONCE THE THING WORKS
#
# Not about mutations.  It is here because this is where the next person reads
# about checking things, and it is the sharpest lesson this project has had.
#
# A DIAGNOSTIC GETS LESS TESTING THAN THE THING IT DIAGNOSES, and its SUCCESS
# branch gets none at all while the thing is failing.  `vivado/fit.tcl` and
# `vivado/bitstream.tcl` both did `get_property SLACK [get_timing_paths ...]`
# unguarded.  That call returns an empty list when there is nothing to report,
# and nothing to report is what meeting timing looks like --- so the first
# design to close timing would have thrown, exited non-zero and printed
# nothing.  Success reading exactly like failure, on the run that proved it
# worked.
#
# One was found by running it.  The other was found by GREPPING FOR THE SAME
# EXPRESSION IN ITS SIBLING, thirty seconds after fixing the first, and it was
# fixed before the run that would have hit it arrived.  When a project has
# been failing at something for a long time, none of its success paths has
# ever executed.  So when you fix a bug in a diagnostic, look for the same
# shape next door instead of waiting for it to happen twice.


# ============================================================== phase gen ===
#
# Held to muir's clock::Behavioural tick for tick, so every instant the
# generator names is a mutation: move one and the trace disagrees.

@mutation tse-falls-late
@check phase_gen
@file rtl/cadr_phase_gen.sv
@note TPTSE goes away at 5 ns and comes back at 25. Moving the fall to 10
@note leaves the pulse the right shape and in the wrong place.
@old
  localparam int unsigned TSE_OFF_T  = 5 / TICK_NS;    //   5 ns
@new
  localparam int unsigned TSE_OFF_T  = 10 / TICK_NS;   //  10 ns
@end

@mutation tse-returns-late
@check phase_gen
@file rtl/cadr_phase_gen.sv
@note The other end of the same pulse.
@old
  localparam int unsigned TSE_ON_T   = 25 / TICK_NS;   //  25 ns
@new
  localparam int unsigned TSE_ON_T   = 30 / TICK_NS;   //  30 ns
@end

@mutation tpr60-opens-early
@check phase_gen
@file rtl/cadr_phase_gen.sv
@note -TPR60 is a window on the phase, 60 to 100 ns, and SPEEDCLK's source.
@note Opening it at 55 is five nanoseconds of a signal that drives the front
@note panel's clock.
@old
  localparam int unsigned TPR60_ON_T = 60 / TICK_NS;   //  60 ns
@new
  localparam int unsigned TPR60_ON_T = 55 / TICK_NS;   //  55 ns
@end

@mutation tpr60-closes-a-tick-late
@check phase_gen
@file rtl/cadr_phase_gen.sv
@note The window is half-open in muir --- `(60..60+TPR_PULSE_NS)` --- so the
@note tick at 100 ns is outside it. An inclusive compare is the off-by-one
@note that a range written as two comparisons invites.
@old
  assign n_tpr60 = !(running && phase >= 6'(TPR60_ON_T) && phase < 6'(TPR60_OFF_T));
@new
  assign n_tpr60 = !(running && phase >= 6'(TPR60_ON_T) && phase <= 6'(TPR60_OFF_T));
@end

@mutation normal-speed-reads-the-fast-tap
@check phase_gen
@file rtl/cadr_phase_gen.sv
@note The 74S151 at CLOCK1 1D08 selects the read phase on {SSPEED1, SSPEED0,
@note -ILONG}. One wrong entry in that table is one wrong microcycle length,
@note and the testbench's tap coverage exists for exactly this.
@old
      SPEED_NORMAL:     read_sel = ilong ? 6'd25 : 6'd17;  // 125 : 85 ns
@new
      SPEED_NORMAL:     read_sel = ilong ? 6'd23 : 6'd17;  // 115 : 85 ns
@end

@mutation extra-slow-stretches-with-ilong
@check phase_gen
@file rtl/cadr_phase_gen.sv
@note At extra slow, 160 ns is already the longest tap the chain provides, so
@note ILONG adds nothing. Making it stretch anyway is the reading of "ILONG
@note adds forty nanoseconds" that forgets the exception.
@old
      SPEED_EXTRA_SLOW: read_sel = 6'd32;                  // 160 ns, both
@new
      SPEED_EXTRA_SLOW: read_sel = ilong ? 6'd40 : 6'd32;  // 200 : 160 ns
@end

@mutation the-read-phase-is-selected-at-the-start-of-the-cycle
@check phase_gen
@file rtl/cadr_phase_gen.sv
@note The 74S151's output is read at 65 ns, not at the start of the cycle,
@note and it only shows if speed or ILONG move between the two --- which the
@note last segment of the trace does deliberately, "speed and ILONG changing
@note inside a cycle". Moved as a constant rather than by replacing the
@note compare with a literal, which would leave SELECT_T unused and let lint
@note catch it instead of the check.
@old
  localparam int unsigned SELECT_T   = 65 / TICK_NS;   //  65 ns
@new
  localparam int unsigned SELECT_T   = 5 / TICK_NS;    //   5 ns
@end

@mutation the-write-pulse-opens-a-tick-late
@check phase_gen
@file rtl/cadr_phase_gen.sv
@note -TPW30, measured from the end of the read phase. The instant lives in
@note a register of its own since 5646c91, so that the counter drives a
@note comparator and nothing else; a tick out is a tick out either way.
@old
      if (phase == wp_on_at) n_tpwp <= 1'b0;
@new
      if (phase == wp_on_at + 6'd1) n_tpwp <= 1'b0;
@end

@mutation the-control-store-pulse-closes-late
@check phase_gen
@file rtl/cadr_phase_gen.sv
@note -TPW45, the one that ends the write to the control store.
@old
  localparam int unsigned WPIRAM_OFF_T = 45 / TICK_NS; // -TPW45
@new
  localparam int unsigned WPIRAM_OFF_T = 50 / TICK_NS; // -TPW50
@end

@mutation the-write-pulse-outlives-the-boundary
@check phase_gen
@file rtl/cadr_phase_gen.sv
@note muir clamps the pulse to the cycle boundary --- `WP_OFF_NS.min(RESTART)`
@note --- because with no gate delays a pulse that outlived it would write at
@note the next instruction's address. Dropping the clamp is the bug that
@note comment is there to prevent.
@old
      if (wrap) n_tpwp <= 1'b1;
@new
@end

@mutation hang-restarts-the-cycle-instead-of-parking-it
@check phase_gen
@file rtl/cadr_phase_gen.sv
@note -HANG holds the next -TPR0 off and the generator parks at cycle_t.
@note Written the other way round it parks when it should run and runs when
@note it should park. Inverted rather than deleted, because deleting the term
@note leaves `hang` unread and lint would catch it instead of the check.
@old
  assign phase_next = wrap ? (hang ? park_at : 6'd0) : phase + 6'd1;
@new
  assign phase_next = wrap ? (hang ? 6'd0 : park_at) : phase + 6'd1;
@end

@mutation the-parked-generator-never-wraps
@check phase_gen
@file rtl/cadr_phase_gen.sv
@note `wrap` has two terms because the park is one tick past the last tick
@note of the cycle: without the second, a generator that -HANG parked would
@note count on past the end when -HANG lifted rather than starting a new
@note cycle. Since 5646c91 the two are separate registers, `wrap_at` and
@note `park_at`, which is a new way to get this wrong --- they can now drift
@note apart, where `cycle_t - 1` and `cycle_t` could not.
@old
  assign wrap       = (phase == wrap_at) || (phase == park_at);
@new
  assign wrap       = (phase == wrap_at);
@end


# ================================================================= cables ===
#
# Two targets hold this one, and it takes both. `cables.pass` is lint, which
# catches a direction or a name; `current` regenerates from muir and catches
# anything else, which is what CLAUDE.md means by "both netlists via
# part::pinout". The runner does the same to its copy.
#
# These are mutations of a *generated* file, so regenerate-and-diff catches
# them by construction. What that proves is that the committed copy cannot
# drift from muir unnoticed --- worth holding, since a checkout builds from
# the committed copy and not from the generator. The mutations with teeth are
# in golden/src/cables.rs, which is the only authority on the port list; those
# need golden/ rebuilt per mutation, and are issue #6 rather than records here.

@mutation a-wire-the-interface-drives-is-made-an-output
@check cables
@file rtl/cadr_cables.svh
@note -MEMACK comes from the bus interface: the processor listens. Making it
@note an output is the direction error the whole generated header exists to
@note prevent, and it is the one kind lint alone can catch.
@old
    input  var logic n_MEMACK,    // -MEMACK
@new
    output var logic n_MEMACK,    // -MEMACK
@end

@mutation a-bidirectional-wire-loses-its-enable
@check cables
@file rtl/cadr_cables.svh
@note Of the 92 wires, 48 are driven from both ends and are carried as value
@note plus enable out with the resolved wire back in, because fabric has no
@note bus to fight over. A wire that lost its enable would be driven always.
@old
    output var logic MEM31_oe,
@new
@end

@mutation a-signal-is-renamed
@check cables
@file rtl/cadr_cables.svh
@note The mangling is settled and inherited by everything generated. A name
@note that drifted from it would still elaborate on its own; what says it has
@note drifted is that the generator writes something else.
@old
    output var logic n_MEMRQ,     // -MEMRQ
@new
    output var logic n_MEMREQ,    // -MEMRQ
@end

@mutation mits-name-for-a-wire-is-changed
@check cables
@file rtl/cadr_cables.svh
@note The comment is MIT's own name, and cadr_cables.map is what ties every
@note identifier back to it. Lint cannot see a comment; only regenerating
@note can. This is the mutation that says `current` is load-bearing and not
@note ceremony.
@old
    output var logic WRCYC,       // WRCYC
@new
    output var logic WRCYC,       // WRCYCLE
@end


# ============================================================ busint, Xbus ===
#
# Held to muir's busint::Busint tick for tick. Three of the four bugs CLAUDE.md
# records against the bus interface are here; the fourth is in the bridge.

@mutation memack-registered-on-a-write
@check busint_xbus
@file rtl/cadr_busint_xbus.sv
@note XACK is made from XBUS ACK IN by the 74S64 at REQLM 0C11 --- a gate.
@note Only a read goes through the 60 ns tap of the TD100 at 0C09. Registering
@note both puts the acknowledgement a tick late on every write.
@old
              || (state == GRANTED && ((write && answering) || deskewed))
@new
              || (state == GRANTED && ((write && answered) || deskewed))
@end

@mutation grant-waits-for-a-later-edge
@check busint_xbus
@file rtl/cadr_busint_xbus.sv
@note A request standing at the master clock edge is granted at that edge:
@note the priority logic registers -MEMRQ and does not care how long it has
@note been up. Making it always wait for the next edge costs a whole
@note microcycle on any request that arrives on one.
@note
@note The block grew the Unibus branch after this was written, and then the
@note two held Unibus instants in place of `ssyn_at`; what the mutation does
@note is unchanged --- every request waits for a later edge, whichever bus it
@note is for. The anchor has to follow the block, which is the loud kind of
@note rot and the reason `@old` is a literal.
@old
            write <= wrcyc;
            if (mclk) begin
              elapsed     <= 10'd0;
              answered    <= 1'b0;
              answered_at <= 10'd0;
              ssyn_seen   <= 1'b0;
              ub_ack_at   <= 10'd0;
              ub_md_at    <= 10'd0;
              tmr_fell    <= 1'b0;
              tmr_rises   <= 3'd0;
              nxm         <= 1'b0;
              if (unibus) begin
                state <= ARB;
                stage <= ub_master ? 3'd4 : 3'd1;
              end else begin
                state <= GRANTED;
              end
            end else begin
              state <= REQUESTED;
            end
@new
            write <= wrcyc;
            state <= REQUESTED;
@end

@mutation timeout-oscillator-restarted-at-the-grant
@check busint_xbus
@file rtl/cadr_busint_xbus.sv
@note The 74LS124 at REQTIM 0A01 has run since the power came up, and the
@note grant only opens its output. Restarting it at the grant is the obvious
@note way to write it and is wrong on every cycle but the lucky ones --- it
@note only shows because the trace's grants land at varied phases.
@old
    if (rst) begin
      vco_count <= 7'd0;
      vco       <= 1'b1;
@new
    if (rst || (mclk && !n_memrq && state != GRANTED && state != ACKED)) begin
      vco_count <= 7'd0;
      vco       <= 1'b1;
@end

@mutation xbus-rq-dropped-at-the-acknowledgement
@check busint_xbus
@file rtl/cadr_busint_xbus.sv
@note "-XBUS.ACK ... remains asserted until the -XBUS.RQ signal is removed by
@note the master": the request stays out through the acknowledgement, and the
@note slave holds its answer against it. A master that dropped it the moment
@note the cycle was acknowledged would break every slave that obeys the
@note specification.
@old
  assign dev_rq    = (state == GRANTED && elapsed >= 10'(SETUP_T)) || state == ACKED;
@new
  assign dev_rq    = (state == GRANTED && elapsed >= 10'(SETUP_T));
@end

@mutation the-setup-time-is-short
@check busint_xbus
@file rtl/cadr_busint_xbus.sv
@note "it is the responsibility of the bus master to assert good address,
@note write, and data lines 80 ns. prior to asserting -XBUS.RQ".
@note
@note AND IT NOW HOLDS A TIMING EXCEPTION HONEST AS WELL, which is worth
@note saying because nothing about the record shows it. `rtl/cadr_machine.xdc`
@note gives `mem_addr`, `mem_wdata` and `mem_write` sixteen ticks to reach the
@note AXI adapter's registers --- on the strength of the same sentence quoted
@note above, and nothing else. An exemption too wide tests nothing and looks
@note exactly like one that is right, and the only way to test its tightness
@note is a mutation of the DESIGN just outside the bound: a constraint cannot
@note be mutated from here, and trying would ask a two-level question.
@note Fifteen ticks where the exception claims sixteen is exactly one tick
@note outside it, which is what this already was.
@note
@note So this record did not change when the exception arrived --- the
@note exception arrived beside this note, in one commit --- and that is the
@note point: the check that catches a fabric that stopped honouring the 80 ns
@note is the same check that would catch a constraint claiming it wrongly.
@old
  localparam int unsigned SETUP_T = 80 / 5;
@new
  localparam int unsigned SETUP_T = 75 / 5;
@end

@mutation the-read-deskew-is-long
@check busint_xbus
@file rtl/cadr_busint_xbus.sv
@note The 60 ns tap of the TD100 at REQLM 0C09, which is what puts the word
@note in MD.
@old
  localparam int unsigned DESKEW_T = 60 / 5;
@new
  localparam int unsigned DESKEW_T = 65 / 5;
@end

@mutation the-deskew-starts-a-tick-late
@check busint_xbus
@file rtl/cadr_busint_xbus.sv
@note `elapsed` still holds the answering tick's value where this runs, which
@note is the sort of thing that is one off as easily as not.
@old
            answered_at <= elapsed;
@new
            answered_at <= elapsed + 10'd1;
@end

@mutation the-timeout-oscillator-runs-fast
@check busint_xbus
@file rtl/cadr_busint_xbus.sv
@note 850 ns, so 425 a half period --- chip::VCO_PERIOD.
@old
  localparam int unsigned VCO_HALF_T = 425 / 5;
@new
  localparam int unsigned VCO_HALF_T = 420 / 5;
@end

@mutation the-timeout-comes-a-period-early
@check busint_xbus
@file rtl/cadr_busint_xbus.sv
@note NXM TIMEOUT is registered on the sixth rise of the gated output, which
@note is the first rise plus busint::TIMEOUT_NS, five whole periods.
@old
  localparam int unsigned NXM_RISES = 6;
@new
  localparam int unsigned NXM_RISES = 5;
@end

@mutation the-timer-starts-on-a-rise-instead-of-a-fall
@check busint_xbus
@file rtl/cadr_busint_xbus.sv
@note The gated output takes its first fall at the oscillator's first fall
@note strictly after the grant, and the rises are counted from there.
@note Waiting for a rise instead moves the whole count a half period on.
@note
@note Worth not re-deriving: the obvious way to write this mutation --- drop
@note the edge test entirely, `if (!tmr_fell)` --- is EQUIVALENT and not a
@note mutation at all. Setting tmr_fell on a rise takes the first branch, so
@note that rise is not counted, and the next rise is a full period later:
@note exactly where it lands if tmr_fell is set at the fall in between. It
@note survives every check because there is nothing to catch.
@old
          if (!tmr_fell && vco) begin
@new
          if (!tmr_fell && !vco) begin
@end

@mutation the-timeout-flag-outlives-its-cycle
@check busint_xbus
@file rtl/cadr_busint_xbus.sv
@note The flag belongs to the cycle standing, as `Ack::timed_out` does: it
@note goes when the cpu lifts -MEMRQ. What outlives the cycle is the NXM bit
@note in the bus error register at REQERR, which is not this slice.
@old
  assign timed_out  = (state == ACKED) && nxm;
@new
  assign timed_out  = nxm;
@end

@mutation no-loadmd-on-a-write
@check busint_xbus
@file rtl/cadr_busint_xbus.sv
@note On the Xbus the word lands with the acknowledgement, so -LOADMD and
@note -MEMACK coincide --- on a write as well, where MD is strobed with
@note nothing new in it. Suppressing it on writes is the plausible-looking
@note economy that the reference does not make.
@note
@note The Unibus term arrived after this was written, and proves the note
@note above: -LOADMD and -MEMACK coincide on the Xbus and do not on the
@note Unibus, where the word comes UNIBUS_STROBE_NS after -UB SSYN. The
@note mutation still suppresses the strobe on a write, on the Xbus half.
@old
  assign n_loadmd   = !(acked || (state == UB && ub_loadmd));
@new
  assign n_loadmd   = !((acked && !write) || (state == UB && ub_loadmd));
@end

@mutation the-cycle-ends-without-waiting-for-the-cpu
@check busint_xbus
@file rtl/cadr_busint_xbus.sv
@note -MEMACK stays low until the cpu lifts -MEMRQ, MFINISHD_NS after the
@note acknowledgement. Leaving ACKED at once makes it one tick wide.
@old
          if (n_memrq) begin
            state <= IDLE;
          end
@new
          state <= IDLE;
@end


# ========================================================= address decode ===
#
# Checked at every one of the 4,194,304 addresses for each board count, and
# checked as well that exactly one answer comes back --- which is the only
# thing that can catch two regions overlapping, because the reference names
# one answer per address and can never say "both".

@mutation the-unibus-starts-a-page-late
@check xbus_decode
@file rtl/cadr_xbus_decode.sv
@note The Unibus half: the diagnostic bus's sixteen registers and the I/O
@note board with its Chaosnet interface.
@old
  assign unibus = page >= 14'o37000;
@new
  assign unibus = page >= 14'o37001;
@end

@mutation xbus-io-space-starts-a-page-late
@check xbus_decode
@file rtl/cadr_xbus_decode.sv
@note The two slots below the Unibus. A page out either way moves the
@note boundary between main memory and the devices.
@old
  assign xbus_io = !unibus && page >= 14'o36000;
@new
  assign xbus_io = !unibus && page >= 14'o36001;
@end

@mutation the-frame-buffer-is-at-the-wrong-slot
@check xbus_decode
@file rtl/cadr_xbus_decode.sv
@note simpletv::BUFFER at 0o17000000 for BUFFER_WORDS: 3,932,160 is 120 times
@note 32,768, which is why this is a comparison on a slice and not a pair of
@note bounds.
@old
  assign tv_buffer  = phys[21:15] == 7'd120;      // 0o17000000, 32768 words
@new
  assign tv_buffer  = phys[21:15] == 7'd121;      // 0o17100000, 32768 words
@end

@mutation the-displays-registers-are-twice-as-wide
@check xbus_decode
@file rtl/cadr_xbus_decode.sv
@note Eight control registers at simpletv::CONTROL, aligned to eight. Reading
@note one bit fewer of the address makes it sixteen, and swallows four words
@note that answer to nothing and one of the disk controller's.
@old
  assign tv_control = phys[21:3]  == 19'd507902;  // 0o17377760, 8 words
@new
  assign tv_control = phys[21:4]  == 18'd253951;  // 0o17377760, 16 words
@end

@mutation the-disk-registers-are-a-block-low
@check xbus_decode
@file rtl/cadr_xbus_decode.sv
@note disk_controller::REGS at 0o17377774 for four. The four words between the
@note display's registers and the disk's answer to nothing, and the reference
@note says so too.
@old
  assign disk_regs  = phys[21:2]  == 20'd1015807; // 0o17377774, 4 words
@new
  assign disk_regs  = phys[21:2]  == 20'd1015806; // 0o17377770, 4 words
@end

@mutation one-memory-board-too-many
@check xbus_decode
@file rtl/cadr_xbus_decode.sv
@note A board is 64K words and they start at zero, so the whole comparison is
@note on the slot number and the bound is exclusive. Inclusive gives the
@note machine a board that is not fitted.
@old
  assign memory = !unibus && !xbus_io && ({1'b0, phys[21:16]} < boards);
@new
  assign memory = !unibus && !xbus_io && ({1'b0, phys[21:16]} <= boards);
@end

@mutation the-page-is-taken-from-the-wrong-bits
@check xbus_decode
@file rtl/cadr_xbus_decode.sv
@note `let page = (phys >> 8) & 0o37777`, and both boundaries come off it.
@old
  assign page = phys[21:8];
@new
  assign page = phys[20:7];
@end

@mutation the-unibus-is-also-nxm
@check xbus_decode
@file rtl/cadr_xbus_decode.sv
@note Exactly one answer, always. Nothing in the reference can catch a decode
@note that gives two, because the reference only ever names one --- which is
@note why the testbench counts them itself. This is the mutation that check
@note exists for.
@old
  assign nxm = !unibus && !memory && !device;
@new
  assign nxm = !memory && !device;
@end


# ============================================================ memory path ===
#
# Two checks at once: the timing still agrees with muir through the whole
# path, and a read returns the word an earlier write put there. The second is
# held to the stimulus and not to anything the DUT produced, which is what
# these first two are here to prove.

@mutation bridge-writes-the-address-instead-of-the-data
@check memory_path
@file rtl/cadr_xbus_ddr.sv
@note The shadow memory was once keyed by the DUT's own mem_addr and filled
@note from its own mem_wdata, so it moved with the bug and this passed. It is
@note keyed by the trace's phys and filled with the trace's wdata now.
@note Written as a ternary rather than as a plain swap because replacing
@note `wdata` outright leaves it unread and lint would catch it instead of
@note the check.
@old
  assign mem_wdata = wdata;
@new
  assign mem_wdata = dev_write ? {10'd0, phys} : wdata;
@end

@mutation bridge-drops-an-address-bit
@check memory_path
@file rtl/cadr_xbus_ddr.sv
@note The other half of the same lesson: a bridge that dropped a bit wrote
@note consistent nonsense, and a shadow keyed by the DUT's address agreed
@note with it on every read. Written as a flip rather than a truncation so
@note that every bit of `phys` stays used.
@old
  assign mem_addr  = main_byte_address(phys);
@new
  assign mem_addr  = main_byte_address(phys ^ 22'd1);
@end

@mutation bridge-answers-at-an-address-nothing-is-at
@check memory_path
@file rtl/cadr_memory_path.sv
@note A cycle to an address with nothing at it reaches no slave and the
@note interface's own timer ends it. A bridge that answered anyway would take
@note the NXM timeout out of the machine altogether.
@note
@note Reads `nxm` where it once read `is_nxm`: the decode's `nxm`
@note became a port of the path rather than a wire inside it when
@note the Xbus was brought out, and the mutation went BROKEN
@note rather than quietly passing --- which is the runner earning
@note its keep on a record that had rotted under a peer's change.
@old
      .sel      (is_memory),
@new
      .sel      (is_memory | nxm),
@end

@mutation every-cycle-is-a-read-on-the-xbus
@check memory_path
@file rtl/cadr_busint_xbus.sv
@note WRCYC is latched for the cycle and goes out to the slave as the bus's
@note write line. Held low, every cycle reaches the slave as a read.
@note
@note Declared against memory_path rather than busint_xbus, and deliberately:
@note the interface's own check compares the four cpu-side outputs, and
@note dev_write means nothing without a slave to take it. The slave is in the
@note memory path, and that is the check that has to notice.
@old
  assign dev_write = write;
@new
  assign dev_write = 1'b0;
@end

@mutation the-bridge-adds-a-tick
@check memory_path
@file rtl/cadr_xbus_ddr.sv
@note The bridge is deliberately thin: dev_ack follows mem_done with no
@note register in the way, so the whole of the latency belongs to the memory
@note behind it and shows up as the device's answer time. A register here is
@note not a correctness bug on the board --- the interface simply waits --- but
@note it is a change to measure, and this is what measures it.
@old
  assign dev_ack = asked && (done || mem_done);
@new
  assign dev_ack = asked && done;
@end

@mutation the-word-is-corrupted-on-its-way-to-md
@check memory_path
@file rtl/cadr_xbus_ddr.sv
@note Read data is latched because the cpu takes it at -LOADMD and the word
@note has to still be there. This is what says the word that arrives is the
@note word that was stored.
@note
@note Worth not re-deriving: latching on a WRITE as well --- dropping the
@note `!dev_write` guard --- is EQUIVALENT at this boundary, and it was
@note filed as a hole (#5) before it was understood. -LOADMD is asserted by
@note the interface on every acknowledgement whatever the direction, and the
@note gate that stops a write strobing MD is the processor's, `if
@note !self.wrcyc`, not the bridge's. So the bridge latching on a write
@note changes nothing anything downstream can see. It is NOT the same as
@note `rdcyc_gate_dropped` in the microcycle check, which drops the
@note processor's own gate and is caught at microcycle 536,307.
@old
      if (!dev_write) rdata <= mem_rdata;
@new
      if (!dev_write) rdata <= mem_rdata ^ 32'd1;
@end

@mutation main-memory-is-a-word-wide
@check memory_path
@file rtl/cadr_ddr_map.sv
@note A CADR word is 32 bits, so the byte address is the word address two
@note places left. This map is shared with the Linux side --- a
@note reserved-memory node, whatever loads a band --- so it is the one thing
@note here that is expensive to get wrong.
@old
    return MAIN_BASE + (32'(phys) << 2);
@new
    return MAIN_BASE + (32'(phys) << 3);
@end

@mutation main-memory-is-reserved-somewhere-else
@check memory_path
@file rtl/cadr_ddr_map.sv
@note The base of the reserved region, which both sides agree on.
@old
  localparam logic [31:0] MAIN_BASE  = 32'h1800_0000;
@new
  localparam logic [31:0] MAIN_BASE  = 32'h1900_0000;
@end


# =========================================================== AXI adapter ===
#
# No muir reference: nothing in MIT's drawings is an AXI master. So it is held
# to AXI's own rules, checked every tick, and to the property that a read
# returns the word a write put there.

@mutation awvalid-held-up-after-awready
@check axi_master
@file rtl/cadr_axi_master.sv
@note A master that holds awvalid up after awready issues a second write, and
@note a slave that only records the address cannot tell. The testbench counts
@note handshakes per transaction and requires exactly one on each channel
@note because of this.
@old
  assign m_axi_awvalid = (state == WRITE) && !aw_sent;
@new
  assign m_axi_awvalid = (state == WRITE);
@end

@mutation master-returns-to-idle-early
@check axi_master
@file rtl/cadr_axi_master.sv
@note The bridge holds mem_req up until it has taken the word, so the answer
@note has to stand until it lets go. A testbench that dropped the request the
@note instant mem_done appeared would never give the master the chance to
@note re-issue, and this would pass.
@old
          if (!mem_req) state <= IDLE;
@new
          state <= IDLE;
@end

@mutation the-write-response-error-is-ignored
@check axi_master
@file rtl/cadr_axi_master.sv
@note OKAY is 00 and EXOKAY 01; SLVERR and DECERR have bit 1 set, and
@note mem_error is what says one came back. Assigning zero rather than
@note deleting the branch, so that bresp stays read and lint has nothing to
@note say about it.
@note
@note This survived until the testbench drove an error at all: with bresp
@note and rresp zero on every tick a master that ignored both was
@note indistinguishable from one that did not. Issue #3.
@old
            if (m_axi_bresp[1]) mem_error <= 1'b1;
@new
            if (m_axi_bresp[1]) mem_error <= 1'b0;
@end

@mutation a-byte-lane-is-masked-off
@check axi_master
@file rtl/cadr_axi_master.sv
@note A whole word, every time: the CADR has no byte writes on this path.
@old
  assign m_axi_wstrb = 4'b1111;
@new
  assign m_axi_wstrb = 4'b1110;
@end

@mutation the-beat-is-not-the-last
@check axi_master
@file rtl/cadr_axi_master.sv
@note At a burst of one every beat is the last. A slave would wait forever
@note for a beat that never comes.
@old
  assign m_axi_wlast = 1'b1;
@new
  assign m_axi_wlast = 1'b0;
@end

@mutation the-transfer-is-eight-bytes-wide
@check axi_master
@file rtl/cadr_axi_master.sv
@note awsize and arsize are 2^2 = 4 bytes, one 32-bit word.
@old
  localparam logic [2:0] SIZE_WORD = 3'b010;  // 2^2 = 4 bytes
@new
  localparam logic [2:0] SIZE_WORD = 3'b011;  // 2^3 = 8 bytes
@end

@mutation the-burst-is-two-beats
@check axi_master
@file rtl/cadr_axi_master.sv
@note awlen and arlen are zero: one beat. The CADR has no way to ask for the
@note next word before it has this one, so bursts would buy nothing.
@old
  localparam logic [7:0] LEN_ONE   = 8'd0;
@new
  localparam logic [7:0] LEN_ONE   = 8'd1;
@end

@mutation the-read-address-is-a-word-on
@check axi_master
@file rtl/cadr_axi_master.sv
@note Latched at IDLE and held for the whole transaction, because AXI wants
@note the payload stable from valid until ready --- and unchanged, because
@note the address is the bridge's.
@old
              m_axi_araddr <= mem_addr;
@new
              m_axi_araddr <= mem_addr + 32'd4;
@end

@mutation the-word-is-corrupted-on-the-way-out
@check axi_master
@file rtl/cadr_axi_master.sv
@note Address and data carried through unchanged is half of what this check
@note holds to. Written as a flip so that mem_wdata stays read.
@old
              m_axi_wdata  <= mem_wdata;
@new
              m_axi_wdata  <= mem_wdata ^ 32'd1;
@end

@mutation the-word-is-corrupted-on-the-way-back
@check axi_master
@file rtl/cadr_axi_master.sv
@note The other direction, which is what the read-back property catches.
@old
            mem_rdata <= m_axi_rdata;
@new
            mem_rdata <= m_axi_rdata ^ 32'd1;
@end

@mutation reads-and-writes-are-the-wrong-way-round
@check axi_master
@file rtl/cadr_axi_master.sv
@note mem_write says which. The coarsest possible bug, and worth having in
@note the list precisely because a check that missed it would be worthless.
@old
            if (mem_write) begin
@new
            if (!mem_write) begin
@end

@mutation the-write-is-answered-before-the-data-is-taken
@check axi_master
@file rtl/cadr_axi_master.sv
@note The two channels are independent and either can be taken first, so the
@note transaction is done when both have been. Waiting only for AW leaves the
@note word unsent.
@old
          if ((aw_sent || (m_axi_awvalid && m_axi_awready))
              && (w_sent || (m_axi_wvalid && m_axi_wready))) begin
@new
          if (aw_sent || (m_axi_awvalid && m_axi_awready)) begin
@end

@mutation the-answer-is-given-before-the-response-comes-back
@check axi_master
@file rtl/cadr_axi_master.sv
@note mem_done is held once the word is done, and a write is not done until B
@note has come back. Answering at WRESP lets the bridge go while the response
@note channel is still outstanding.
@note
@note This survived until the testbench said when mem_done was allowed to
@note rise. Breaking no AXI rule and returning the right words, it simply
@note finished early --- 28,548 transactions against 25,918. Issue #4.
@old
  assign mem_done = (state == DONE);
@new
  assign mem_done = (state == DONE) || (state == WRESP);
@end


# ============================================================ the widening ===
#
# `rtl/cadr_axi_widen.sv`: the machine's 32-bit word in `S_AXI_HP0`'s 64-bit
# beat.  Six assignments, and until they were pulled out into a module they
# lived in `rtl/cadr_arty.sv`'s `g_ddr`, where nothing could reach them --- the
# top level has an `MMCME2_BASE` and a `PS7` in it and Verilator has neither,
# so lint and the fitter were the whole of the evidence.  Every mutation below
# builds, lints, fits and programs; none of them is anything but a machine that
# reads back the wrong word.
#
# WHAT THE CHECK IS.  `tb/cadr_axi_widen_tb.cpp` applies the DUT's own `m_wdata`
# and `m_wstrb` into a model beat the *stimulus* chose the index of, and
# compares the whole beat --- both halves --- against a shadow the stimulus
# filled.  The memory is poisoned rather than zeroed, so a wrong lane always has
# a wrong answer to return, and the two channels are driven independently, so a
# lane selected from the wrong one of them shows.  The one thing that structure
# cannot catch is an address bug --- a model keyed by the DUT's address moves
# with it --- so the beat address is compared directly against what the
# stimulus address should have produced, over addresses whose every bit above
# the beat boundary takes both values.

@mutation the-read-lane-is-taken-from-the-wrong-half
@check axi_widen
@file rtl/cadr_axi_widen.sv
@note The lane select inverted: a read of the low word gives the high one and
@note the other way about. Both halves stay read, so lint has nothing to say
@note about it, and on the board it is a machine whose every other main-memory
@note read is a different word.
@note
@note This is the mutation the poison exists for. Against a model memory of
@note zeros the first read of any beat returns zero whichever half it takes,
@note and it is only the second that could disagree.
@old
  assign s_rdata = s_araddr[2] ? m_rdata[63:32] : m_rdata[31:0];
@new
  assign s_rdata = s_araddr[2] ? m_rdata[31:0] : m_rdata[63:32];
@end

@mutation the-read-lane-is-selected-by-the-write-address
@check axi_widen
@file rtl/cadr_axi_widen.sv
@note Each direction has to take its own address: the strobes are placed by
@note `s_awaddr` and the lane is selected by `s_araddr`, because those are the
@note addresses the two transactions are at. `cadr_axi_master` holds each
@note until the next transaction of that kind replaces it, so on a read
@note `awaddr` is some earlier write's address and agrees with `araddr` about
@note bit 2 half the time. A testbench that drove one address into both
@note channels would never see this.
@note
@note Written as an XOR rather than as a substitution: replacing `s_araddr[2]`
@note outright leaves it unread everywhere --- the beat address takes 31:3 ---
@note and Verilator would reject the mutation instead of the check catching it.
@old
  assign s_rdata = s_araddr[2] ? m_rdata[63:32] : m_rdata[31:0];
@new
  assign s_rdata = (s_araddr[2] ^ s_awaddr[2]) ? m_rdata[63:32] : m_rdata[31:0];
@end

@mutation the-write-reaches-both-halves-of-the-beat
@check axi_widen
@file rtl/cadr_axi_widen.sv
@note The strobes are the whole of what keeps a write out of the half it is
@note not addressed to: the word is offered in both halves of the beat, so a
@note strobe pattern that opens both writes the neighbouring word as well.
@note Silent on the board --- every read of the addressed word is right, and
@note the word beside it is quietly destroyed.
@note
@note One arm only, so `s_awaddr[2]` stays read. The check catches it twice
@note over, once as a strobe pattern and once as the beat the write left
@note behind, and both messages are printed.
@old
  assign m_wstrb = s_awaddr[2] ? {s_wstrb, 4'b0000} : {4'b0000, s_wstrb};
@new
  assign m_wstrb = s_awaddr[2] ? {s_wstrb, s_wstrb} : {4'b0000, s_wstrb};
@end

@mutation the-half-selecting-bit-leaks-into-the-beat-address
@check axi_widen
@file rtl/cadr_axi_widen.sv
@note The beat address is the word's with the low three bits cleared. Clearing
@note two of them leaves bit 2 in it, so the port is asked for a beat at a
@note four-byte boundary --- which on a 64-bit AXI3 port with `AWSIZE` of eight
@note is an unaligned transfer, and the strobes below have already accounted
@note for that bit. The word would land four bytes on.
@note
@note The only mutation here that the read-back property cannot catch, and the
@note reason the testbench compares the address directly: a model memory keyed
@note by the address the DUT presented would write consistent nonsense at the
@note wrong beat and read it back from the same wrong beat. CLAUDE.md's shadow
@note memory lesson, in the one place in this module where it applies.
@old
  assign m_awaddr = {s_awaddr[31:3], 3'b000};
@new
  assign m_awaddr = {s_awaddr[31:2], 2'b00};
@end

@mutation the-word-is-offered-in-only-one-half-of-the-beat
@check axi_widen
@file rtl/cadr_axi_widen.sv
@note The word goes into both halves of the beat and the strobes choose which
@note copy lands. Offering it only in the low half makes every write to an odd
@note word write zero, and only that: the strobes are still right, so the
@note transaction is legal AXI and the port takes it without complaint.
@old
  assign m_wdata = {s_wdata, s_wdata};
@new
  assign m_wdata = {32'd0, s_wdata};
@end

@mutation the-beat-says-it-is-a-word-wide
@check axi_widen
@file rtl/cadr_axi_widen.sv
@note `AWSIZE` and `ARSIZE` are the beat's and not the word's: eight bytes,
@note which is what makes a full-width beat with four strobes the way a
@note 32-bit word crosses a 64-bit port. Saying four bytes while driving
@note strobes in the upper half is a narrow transfer whose strobes are outside
@note the transfer, which is not legal AXI at all --- and which the adapter's
@note own `s_awsize` says, so this is exactly the mistake of deriving the
@note beat's size from the word's.
@old
  localparam logic [1:0] SIZE_BEAT = 2'b11;
@new
  localparam logic [1:0] SIZE_BEAT = 2'b10;
@end

@mutation the-burst-length-the-port-takes-is-the-wrong-nibble
@check axi_widen
@file rtl/cadr_axi_widen.sv
@note AXI4 carries eight bits of burst length and AXI3 four, so the narrowing
@note is a truncation. Rotating the nibble rather than taking another one,
@note because `s_arlen[7:4]` is already accounted for in the unused fold and
@note taking it here would leave `s_arlen[3:0]` unread --- lint would reject
@note the mutation and the check would never see it.
@note
@note Both lengths are zero on every transaction the adapter makes, so this
@note breaks nothing on the board today. What it says is whether the check
@note holds the truncation over the whole range or only over the value the
@note design happens to use, which is the difference between a check and a
@note coincidence.
@old
  assign m_arlen = s_arlen[3:0];
@new
  assign m_arlen = {s_arlen[2:0], s_arlen[3]};
@end


# ============================================ the generators, on themselves ===
#
# Everything above mutates the fabric and asks whether a check notices. These
# two ask something else: whether the *generator's own assertions* are live.
#
# They are here because nothing downstream can stand in for them. `cables.rs`
# is the only authority on the port list --- regenerate-and-diff catches any
# edit to what it emits, but not an error in the emitting --- and
# `busint_xbus.rs` writes the stimulus and the expected outputs both, so a
# trace agreeing with itself proves nothing at all. In each the guard is an
# assertion inside the generator, and `make mutants` is the only thing that
# can ask whether it fires.
#
# The check for these is `cargo run`: the generator refusing to write is the
# mutation being caught. Build and run are separate, so a mutation rustc
# rejects is BROKEN and not caught.
#
# WHAT CANNOT BE MUTATED HERE, and it is worth saying rather than leaving as a
# gap someone else notices. Deleting an assertion is undetectable by running
# the program: an assertion that guards good input does nothing when the input
# is good, so removing it changes no output. What can be tested is whether the
# assertion catches a *wrong derivation* or a *wrong table*, which is what
# these do. An assertion nothing below trips is one nothing here can vouch for.

@mutation the-prose-table-disagrees-with-the-netlists
@check cables_gen
@file golden/src/cables.rs
@note cable.rs's own table names nine signals and says which end drives each.
@note cables.rs reproduces it and stops if the netlists disagree, because
@note where the prose and the netlists part one of them is wrong. -MEMACK
@note comes from the bus interface and the processor listens; saying the cpu
@note drives it too is the disagreement that has to stop the run.
@old
        ("-MEMACK", false, true),
@new
        ("-MEMACK", true, true),
@end

@mutation a-totem-pole-output-is-not-counted-as-a-driver
@check cables_gen
@file golden/src/cables.rs
@note The direction of every one of the 92 wires comes from `part::pinout`
@note and `drive_of`: a wire with no driver on a side is one that side only
@note listens to. Dropping the plain totem-pole case makes drivers into
@note listeners wholesale --- the derivation wrong rather than the table ---
@note and the nine-signal cross-check is what has to notice.
@old
                Some(Drive::Totem) => any = true,
@new
                Some(Drive::Totem) => {}
@end

@mutation a-pull-up-is-counted-as-a-driver
@check cables_gen
@file golden/src/cables.rs
@note The other direction: "a pull-up is not a driver, and neither is a
@note connector pin". Counting them makes listeners into drivers, which
@note would turn inputs into the value-and-enable pair a bidirectional wire
@note gets and quietly widen the port list.
@old
                Some(Drive::PullUp | Drive::Passive) | None => {}
@new
                Some(Drive::PullUp | Drive::Passive) | None => any = true,
@end

@mutation the-addresses-share-a-factor-with-the-write-period
@check busint_gen
@file golden/src/busint_xbus.rs
@note CLAUDE.md's fifth lesson, and the one bug of the eight with no mutation
@note of the fabric: addresses visited in rotation with a write every third
@note cycle, and an address count sharing a factor with three puts writes on
@note one residue and reads on the others. The integrity check then passes
@note while testing nothing, with every timing comparison green. Dropping one
@note of the sixteen leaves fifteen, and fifteen shares three.
@old
    0o777,
@new
@end

@mutation the-addresses-share-a-factor-with-the-unanswered-period
@check busint_gen
@file golden/src/busint_xbus.rs
@note The same trap on the other period: a cycle in five is left unanswered,
@note so a count divisible by five puts the timeouts on one residue for ever.
@note Twenty addresses would share five --- here four of the sixteen are
@note duplicated to reach it, which keeps every address a real one.
@old
    MEMORY_WORDS - 2,
@new
    MEMORY_WORDS - 2,
    0,
    1,
    2,
    3,
@end

@mutation the-read-response-error-is-ignored
@check axi_master
@file rtl/cadr_axi_master.sv
@note The other half of what issue #3 left untested. A read carries its
@note response on rresp, and a slave that cannot answer says so there.
@old
            if (m_axi_rresp[1]) mem_error <= 1'b1;
@new
            if (m_axi_rresp[1]) mem_error <= 1'b0;
@end

@mutation an-error-is-reported-when-there-was-none
@check axi_master
@file rtl/cadr_axi_master.sv
@note The opposite failure to ignoring an error, and the one a check that
@note only ever saw good responses could never distinguish: reporting a
@note failure on every transaction that worked. Inverted rather than made
@note unconditional, so bresp stays read.
@old
            if (m_axi_bresp[1]) mem_error <= 1'b1;
@new
            mem_error <= !m_axi_bresp[1];
@end

@mutation the-error-outlives-its-transaction
@check axi_master
@file rtl/cadr_axi_master.sv
@note mem_error is cleared as each transaction is latched, so it belongs to
@note the one being answered. Left standing it is sticky: one refused write
@note and every read after it looks refused too, which is worse than not
@note reporting the error at all.
@old
            mem_error <= 1'b0;
@new
@end

@mutation the-cycle-timer-wraps-under-a-long-request
@check busint_xbus
@file rtl/cadr_busint_xbus.sv
@note `elapsed` gates -XBUS.RQ through SETUP_T. Ten bits of it wrap at 1,024
@note ticks, and the request then falls for the sixteen it takes to count
@note back up --- in the middle of a cycle, on a signal that is a level on
@note the board and cannot fall there at all.
@note
@note This was a real bug, and this list is how it was found: writing the
@note check for issue #2 made it fail on the good design nine times in
@note 40,000 ticks, each exactly 1,024 ticks after a grant. Only a cycle
@note nothing answers runs long enough to reach it --- the NXM timer ends
@note one at about 935 ticks plus the oscillator's phase --- so no slave was
@note ever listening when it happened, and every output agreed.
@note
@note The anchor carries three lines it does not change, and has to. Since
@note the arbitration landed, the Unibus state increments `elapsed` under
@note the same guard and then tests `acked` and sets ACKED in exactly the
@note same words --- four identical lines --- and only the `else if` after
@note them differs. The increment alone matched twice and the run refused
@note it, which is the exactly-once rule doing what it was chosen over
@note `sed` to do: a mutation that silently applies to the wrong place is
@note the same false finding as one that silently fails to apply, pointing
@note the other way. Anchored on code rather than on the comment above it,
@note so the prose can be rewritten without breaking this.
@old
          if (elapsed != 10'h3FF) elapsed <= elapsed + 10'd1;
          if (acked) begin
            state <= ACKED;
          end else if (answering && !answered) begin
@new
          elapsed <= elapsed + 10'd1;
          if (acked) begin
            state <= ACKED;
          end else if (answering && !answered) begin
@end


# ========================================================== the processor ===
#
# Two checks, one module: `microcycle` runs MIT's boot PROM and
# `microcycle_sys` a System 100 band. A mutation goes to the cheaper one
# unless the band is the only thing that reaches what it breaks --- the map,
# the dispatch memory's read, Q's shifter, the stack RAM, SINTR and -ILONG,
# which the boot PROM never touches. 600,000 microcycles against 2,200,000,
# and 84 MB against 297.
#
# NOT HERE YET: anything in the stall path, or touching `md`, `MBUSY` or
# `-LOADMD`. The processor is being composed with the memory path --- `md`
# stops being a trace column and comes from the real bus interface --- and
# mutations written against a design about to move are work done twice. That
# is where the tolerances live too: the sub-tick slip on stalled microcycles,
# the Unibus arbitration exemption, and the USE.MD discrimination.

@mutation the-a-memory-pass-around-is-inverted
@check microcycle
@file rtl/cadr_microcycle.sv
@note A scratchpad write is one microcycle behind the instruction that
@note computed it, and the pass-around is what hides that from the microcode:
@note the comparators at ACTL 3B21/3B27 match the source address against the
@note pending WADR and put L on the bus instead of the memory's output. Taken
@note the wrong way round it reads the stale word exactly when it must not,
@note and the fresh one only when the memory already had it.
@old
  assign a = apass ? l : amem_q;
@new
  assign a = apass ? amem_q : l;
@end

@mutation the-a-memory-pass-around-never-fires
@check microcycle
@file rtl/cadr_microcycle.sv
@note The comparator inverted rather than the mux: the pass-around fires on
@note every address but the one it is for. Inverted rather than deleted so
@note that `aadr` stays read.
@old
  assign apass = destd && (wadr == aadr);
@new
  assign apass = destd && (wadr != aadr);
@end

@mutation the-m-memory-pass-around-ignores-what-the-write-was
@check microcycle
@file rtl/cadr_microcycle.sv
@note The 93S46 at MCTL 4B18 matches five bits of the pending write address
@note and `DESTMD` as its sixth. Without that sixth bit an A-only write
@note passes its word around to an M source whose address happens to match
@note the low five bits, and the microcode reads a word that was never
@note written where it is reading.
@note
@note Worth not re-deriving: the *other* obvious mutation here --- comparing
@note all ten bits, `wadr == {5'd0, madr}` --- is EQUIVALENT and not a
@note mutation at all. `destmd` and `wadr` are registered at the same edge
@note from the same instruction, and `wadr_in` is `{5'd0, ir[18:14]}`
@note whenever `destm`, so `wadr[9:5]` is zero on every cycle `destmd` is
@note set. It survives both processor checks because there is nothing to
@note catch.
@old
  assign mpass = destmd && (wadr[4:0] == madr);
@new
  assign mpass = (wadr[4:0] == madr);
@end

@mutation the-dispatch-mask-is-a-bit-too-wide
@check microcycle_sys
@file rtl/cadr_microcycle.sv
@note `IR<7:5>` is a length and the mask is `(1 << len) - 1`. Without the
@note minus one every dispatch takes one more bit of R than it asked for.
@note On the band, because the boot PROM writes the dispatch memory
@note 2,048 times and reads it never: a wrong address there is a wrong
@note place to write and no one ever looks. Confirmed --- it survives
@note `microcycle` and is caught by `microcycle_sys`.
@old
  assign dmask  = (8'd1 << ir[7:5]) - 8'd1;
@new
  assign dmask  = (8'd1 << ir[7:5]);
@end

@mutation a-map-dispatch-is-recognised-by-the-wrong-gate
@check microcycle_sys
@file rtl/cadr_microcycle.sv
@note `-DMAPBENB = NOR(IR8, IR9)` at DSPCTL 3F14, so a dispatch takes a map
@note bit *instead of* R0 whenever either bit is set. Read as a NAND it takes
@note the map bit only when both are, and R0 comes back in the rest of the
@note time --- which is the "easy misreading" the source names, arrived at
@note through the gate rather than through the OR.
@note Same routing and the same reason: no dispatch is ever read out of
@note the boot PROM's 2,048 writes. Survives `microcycle`, caught by
@note `microcycle_sys`.
@old
  assign dmap   = ir[8] || ir[9];
@new
  assign dmap   = ir[8] && ir[9];
@end

@mutation the-second-map-level-is-read-at-the-wrong-entry
@check microcycle_sys
@file rtl/cadr_microcycle.sv
@note Both levels are asynchronous and chained combinationally: a lookup is
@note a ripple through two RAMs inside one cycle, the first level's five bits
@note standing above the low five of `MAPI` to address the second. Read one
@note entry off and every translation is plausible and wrong.
@note
@note WHY THE READ AND NOT `adr1` ITSELF, which is the mutation to reach for:
@note `adr1` addresses the write as well as the read. Change it and the map
@note is permuted consistently --- written to the wrong entry, read from the
@note same wrong entry --- and since a rotation is a bijection the mapping
@note from `MAPI` to `VMO` is untouched. It is a relabelling of the storage
@note and nothing can see it. Measured before it was understood: the map
@note word differs on 1,649,965 of 4,400,000 lookups and the permission bits
@note on 1,204,657 of them, and the check still passed --- because the DUT
@note was self-consistent, not because the check was blind. A memory's
@note indexing can only be mutated by breaking its read differently from its
@note write.
@note
@note The boot PROM reads the map once in 600,000 microcycles, so this is
@note the band's to catch or nobody's.
@old
  assign vmo  = l2_map[adr1];
@new
  assign vmo  = l2_map[{adr1[8:0], adr1[9]}];
@end

@mutation the-first-map-level-is-read-at-the-wrong-entry
@check microcycle_sys
@file rtl/cadr_microcycle.sv
@note The other half of the same chain, and the same reasoning about which
@note signal to mutate: eleven bits of `MAPI` pick the level-1 entry, and
@note `adr0` addresses that RAM's write too.
@old
  assign vmap = l1_map[adr0];
@new
  assign vmap = l1_map[{adr0[9:0], adr0[10]}];
@end

@mutation the-scratchpad-latches-follow-in-the-wrong-phase
@check microcycle_sys
@file rtl/cadr_microcycle.sv
@note The third of the three things `rtl.rs`'s header calls easy to model one
@note step out of place, and the one the other two mutations here do not
@note reach. The 93425As have no clock pin: what holds a word between phases
@note is the 74S373 at ALATCH, MLATCH, PLATCH or SPCLCH, which follows the
@note memory while CLK is HIGH and holds through the write phase.
@note "Modelling the memory as a register loaded early is the 74S373 drawn
@note one state too soon." Following in the write phase instead is that
@note drawing, exactly: every source reads the cycle before's word, and the
@note write pulses now land while the latches are still following, which is
@note the read-during-write question the real phasing settles for nothing.
@note
@note On the band, and measured rather than reasoned: the boot
@note PROM does not catch this and the band does. Six hundred
@note thousand microcycles of a program that loads the control
@note store leave the scratchpads reading the same few words
@note often enough that a phase's staleness does not show.
@old
    if (tpclk) begin
      amem_q <= amem[aadr];
      mmem_q <= mmem[madr];
      pdl_q  <= pdl[pdla_read];
      spc_q  <= spcm[spcptr];
    end
@new
    if (!tpclk) begin
      amem_q <= amem[aadr];
      mmem_q <= mmem[madr];
      pdl_q  <= pdl[pdla_read];
      spc_q  <= spcm[spcptr];
    end
@end

@mutation the-stacks-push-pass-around-is-inverted
@check microcycle
@file rtl/cadr_microcycle.sv
@note The third pass-around, and the one that is not a comparator: while a
@note push is pending, `SPCWPASS` at CONTRL 3D21 turns the 74S241s at
@note 4E17/4E18 on and `-SPCPASS` turns the latch off, so the word about to
@note be written stands on the SPC bus instead of the RAM's. Taken the wrong
@note way round, a push reads the stack under the word it is pushing.
@note
@note This is also why the stack RAM is so nearly untestable on the boot
@note PROM: every push is popped by the very next microcycle through this
@note pass-around, so the RAM is written and never read. It is caught here
@note because the pass-around itself is on the datapath.
@old
  assign spcv = spushd ? spcw : spc_q;
@new
  assign spcv = spushd ? spc_q : spcw;
@end

@mutation the-first-map-level-is-read-a-microcycle-stale
@check microcycle_sys
@file rtl/cadr_microcycle.sv
@note "THERE IS NO MMU STATE." Both levels are asynchronous and chained
@note combinationally --- VMEM0 1C14 drives `-VMAP4..0`, which are VMEM1
@note 1E04's address --- so a lookup is a ripple through two RAMs inside one
@note cycle. Delaying the first level by twenty-eight ticks is a microcycle
@note of staleness: the lookup belongs to the cycle before the one using it.
@note
@note WHY TWENTY-EIGHT AND NOT ONE, which is the mutation to reach for.
@note Registering the read --- one tick --- is EQUIVALENT, and so is any
@note delay below a microcycle. `MAPI` is `VMA<23:8>` or `MD<23:8>`, both
@note microcycle registers, so the level-1 address is constant for the whole
@note microcycle; a synchronous read of a constant address gives the same
@note word at every sampling point more than a tick after it settles, and
@note the read phase is 29 ticks at normal speed and 44 at extra slow.
@note
@note Found by sweeping the delay rather than asking whether one delay is
@note caught: 1, 4 and 16 ticks survive; 28 is caught on PCS0 at microcycle
@note 2,084,731 and 44 on JCOND at 1,062,763. So the check is blind only
@note *below one microcycle*, which is precisely where staleness is not real.
@note An equivalent mutant that must be explained forever is worth less than
@note a live one testing the same property, so the smallest magnitude that
@note is caught is what the list carries.
@note
@note WHAT THIS DOES NOT SAY, and it matters at bring-up: it does not say
@note the fabric may not register the map. Functionally it may --- that is
@note what the equivalence means. The lookup is combinational because
@note 2048 x 5 and 1024 x 24 fit in distributed RAM, which reads
@note asynchronously and costs nothing. If fit ever forced the map into
@note block RAM, THIS CHECK WOULD NOT OBJECT. That has to be decided from
@note the timing, not from here.
@old
  assign vmap = l1_map[adr0];
@new
  logic [4:0] vmap_d [0:27];
  always_ff @(posedge clk) begin
    vmap_d[0] <= l1_map[adr0];
    for (int d = 1; d < 28; d++) vmap_d[d] <= vmap_d[d-1];
  end
  assign vmap = vmap_d[27];
@end

@mutation the-map-is-written-with-the-wrong-frame
@check microcycle_sys
@file rtl/cadr_microcycle.sv
@note MAPWR1D is `WMAPD AND VMA25` at VCTL2 1C15 and the level-2 word is
@note VMA<23:0> --- the frame the translation returns. Corrupting what is
@note stored asks the plainest possible question about the map: is anything
@note the check compares downstream of what the map holds?
@old
        if (vma[25]) l2_map[adr1] <= vma[23:0];
@new
        if (vma[25]) l2_map[adr1] <= vma[23:0] ^ 24'd1;
@end

@mutation the-read-finish-delay-is-charged-in-full
@check microcycle
@file rtl/cadr_microcycle.sv
@note `-RDFINISH` is `-MFINISH` through the TD50 at VCTL1 1D23 and the TD250
@note at 1D22 --- 140 ns on the tap ordering --- and the fabric charges two
@note ticks fewer, deliberately: ending a hang costs it two ticks the board
@note spends in gate propagation, one for the parked generator to see -HANG
@note lift and one to raise TPCLK.
@note
@note A number chosen to make a check pass is exactly what a mutation list
@note exists to interrogate. Charged in full, the claim is that every one of
@note the boot PROM's 11,404 hangs ends late --- so if this survives, the
@note shortfall was never load-bearing at all.
@note
@note The shortfall is derived for two of its three ticks and measured for
@note the third --- see `the-read-finish-delay-is-a-tick-longer`, which also
@note records that the explanation this list used to give for that third
@note tick has been disproved.
@old
  localparam int unsigned RD_FINISH_T = (140 / 5) - 3;
@new
  localparam int unsigned RD_FINISH_T = (140 / 5);
@end

@mutation the-read-finish-delay-is-a-tick-longer
@check machine
@file rtl/cadr_microcycle.sv
@note The boundary is `memack + N + 2`: the countdown expires at tick X,
@note RD.IN.PROGRESS is visible from X+1, the parked generator samples -HANG
@note pre-edge at the posedge ending X+1 and raises TPCLK. Wanting `memack +
@note 28` gives N = 26, which is two ticks short of 140.
@note
@note The constant is THREE short and the derivation accounts for two. This
@note record used to be named for the third --- it claimed the tick belonged
@note to a testbench placing -MEMACK a tick late, and that correcting the
@note placement would make `- 2` exact. **That has been measured and it is
@note false.** The placement really was a tick late, on every write, and
@note correcting it collapses the acknowledgement error to sub-tick; `- 2` is
@note still five nanoseconds long on all 11,301 reads either way, and `- 3`
@note is exact either way. The name went with the story.
@note
@note What the record still holds is what it always tested: this constant is
@note load-bearing to a tick on the composed check, and the check says so on
@note 11,301 microcycles. Why 28 - 3 rather than the derived 28 - 2 is open,
@note and is issue #11.
@note
@note ON THE COMPOSED CHECK, AND MEASURED RATHER THAN ASSUMED. `- 2` makes
@note the STANDALONE exact --- its 11,301 sub-tick slips go to zero --- so it
@note survives there and says nothing. It is the composed check that pays,
@note because it has the real bus interface under it rather than a model of
@note one, and a record has to sit on the check that pays.
@old
  localparam int unsigned RD_FINISH_T = (140 / 5) - 3;
@new
  localparam int unsigned RD_FINISH_T = (140 / 5) - 2;
@end

@mutation a-hang-ends-a-tick-late-on-the-machine
@check machine
@file rtl/cadr_microcycle.sv
@note The composed check's own edge, and it has moved back twice. It
@note exempted a WHOLE tick for one commit, which is what let a
@note microcycle-early grant pass as a five-nanosecond length slip; the
@note exemption then went back under a tick, and now there is none at all ---
@note the check requires every length exact and guards that count at zero.
@note So one tick is comfortably outside it, which is what this carries.
@note
@note Kept as a record of its own rather than left to the standalone's,
@note because the two checks measure against different references --- the
@note standalone's -MEMACK is its own model, the composed one's comes from
@note the real bus interface --- and an edge is only tested on the check that
@note has it.
@old
  localparam int unsigned RD_FINISH_T = (140 / 5) - 3;
@new
  localparam int unsigned RD_FINISH_T = (140 / 5) - 4;
@end

@mutation use-md-does-not-mind-a-nopped-cycle
@check microcycle_sys
@file rtl/cadr_microcycle.sv
@note `USE.MD` is `NOR(-SRCMD, NOPA)` at VCTL1 3F18: this instruction reads
@note MD, and a nopped cycle reads nothing. Without the gate a nopped
@note instruction whose source field happens to name MD stalls the machine
@note for a word it will not use.
@note
@note On the band, measured: the boot PROM does not catch it and
@note the band does. A nopped cycle naming MD as its source is
@note not something the PROM's own program does.
@old
  assign use_md  = srcmd && !nop;
@new
  assign use_md  = srcmd;
@end

@mutation a-write-to-vma-is-taken-as-a-write-to-md
@check microcycle
@file rtl/cadr_microcycle.sv
@note `DESTMDR` is the destination decode's bit 22: of the two memory
@note destinations, VMA is bit 22 low and MD bit 22 high. Ignoring the bit
@note makes every VMA write an MD write as well.
@old
  assign destmdr = destmem && ir[22];
@new
  assign destmdr = destmem;
@end

@mutation the-wait-term-that-never-fires-on-a-read
@hole #1
@check microcycle
@file rtl/cadr_microcycle.sv
@note One of the three terms of -WAIT at VCTL1 3F16, and one of the two that
@note never fire on either program: the first term, `DESTMEM AND
@note MBUSY.SYNC`, carries the whole stall path on everything observed. So
@note this cannot be caught, and issue #1 is what holds that open --- it is
@note the same finding as the dispatch read and the statistics counter, a
@note piece of the machine no program reached.
@note
@note It stays in the list rather than being dropped: when a program does
@note exercise it, this is what will say so, by starting to fail.
@note
@note The anchor follows the line: the IR-derived half of each term is now
@note held a tick, so `use_md` here reads `use_md_q`. What the mutation does
@note is unchanged --- the term is suppressed with a constant, in the one
@note expression MACHRUN and -HANG both read.
@old
              || (use_md_q && mbusy && n_memgrant)
@new
              || (use_md_q && mbusy && n_memgrant && 1'b0)
@end

@mutation the-wait-term-that-never-fires-on-a-fetch
@hole #1
@check microcycle
@file rtl/cadr_microcycle.sv
@note The other of the two: `LCINC AND NEEDFETCH AND MBUSY.SYNC`. Suppressed
@note with a constant rather than deleted, so that every signal stays read
@note and lint has nothing to say --- a term removed outright would take
@note `ifetch_q` out of use with it.
@note
@note The anchor follows the line twice over. `LCINC AND NEEDFETCH` is what
@note `ifetch` already means --- `assign ifetch = needfetch && lcinc` --- and
@note the term is now written with that name and held a tick, so the line
@note reads `ifetch_q && mbusy_sync`. What the mutation does is unchanged.
@old
              || (ifetch_q && mbusy_sync);
@new
              || (ifetch_q && mbusy_sync && 1'b0);
@end


# ================================================== the machine, composed ===
#
# The processor with the memory path under it and nothing driven nearer than
# `mem_req`/`mem_done`. `cadr_machine.sv` is pure wiring, which is exactly
# what makes this check the only one that can catch a cable crossed: both
# halves are right on their own and the machine is not. Neither `microcycle`
# nor `memory_path` can see any of these, because each has the other half as
# a testbench rather than as a design.
#
# It agrees for all 600,000 microcycles of the boot PROM, and 17,466 of those
# carry a bus cycle: 512 to main memory, 16,951 to the disk controller's four
# registers, two to empty Xbus space and one to the Unibus. The processor
# computes for its first half million microcycles and then does nothing but
# poll the disk, so main memory is proved on 512 cycles rather than on many of
# them, and the Unibus on exactly one. Aim accordingly.
#
# The 512 and the 1 are what this section's records mostly reach; the 16,951
# have a section of their own below, and the caveat that goes with them.

@mutation the-write-line-is-driven-by-the-read-cycle
@check machine
@file rtl/cadr_machine.sv
@note WRCYC and RDCYC are separate cables and the bus interface takes WRCYC.
@note Crossed here, every cycle reaches the memory path as the opposite of
@note what the processor asked for. Both modules are unchanged and correct;
@note only the wire between them is wrong, which is the one class of bug a
@note composition exists to find.
@old
      .wrcyc      (wrcyc),
      .phys       (phys),
@new
      .wrcyc      (rdcyc),
      .phys       (phys),
@end

@mutation the-grant-comes-a-microcycle-early
@check machine
@file rtl/cadr_microcycle.sv
@note MCLK7 has to coincide with the edge the processor's own registers move
@note on, not follow it. `-MEMRQ` is `MEMSTART AND VMAOK OR MBUSY` and
@note MEMSTART is registered at the edge *before* the cycle runs, so the
@note priority logic samples -MEMRQ still high at that edge and grants at
@note the next --- the edge the cycle starts on. Registered, MCLK arrives a
@note tick late, sees -MEMRQ already low, and grants a whole microcycle
@note early.
@note
@note THE SIZE IS THE POINT, and it took three wrong answers to find it. A
@note grant one tick late does NOT matter and is not a weak check: -MEMRQ is
@note up for the whole preceding microcycle, so anything inside that window
@note grants. A grant a microcycle out does matter, and only the Unibus can
@note tell, because arbitration begins at the grant and has to know then
@note which bus it is on. This was a real bug: it put the mode register's
@note write 220 ns early and set PROMDISABLE one microcycle before muir
@note does, measured on the band at 1,410,034.
@note
@note So the mutation is `phase_next == X` sized wrong all over again: not
@note whether the check sees a difference, but at what magnitude. Sweeping
@note found a tick invisible and a microcycle caught, and the tick turned
@note out to be correct behaviour rather than a hole.
@note
@note THE SWEEP WAS TAKEN BEFORE ffd1c96 AND CANNOT BE REPRODUCED AFTER IT.
@note With MCLK registered, inverting it moved the grant one tick; with MCLK
@note combinational it moves it a microcycle, and the inverted mutation is
@note caught. The reading that survives is the one about magnitudes --- a
@note tick inside the window -MEMRQ is up for cannot matter, a microcycle
@note can --- and not the particular mutation the sweep used to show it.
@old
  assign mclk = mclk_edge;
@new
  logic mclk_q;
  always_ff @(posedge clk) mclk_q <= mclk_edge;
  assign mclk = mclk_q;
@end

@mutation the-data-cable-back-from-memory-is-rotated
@check machine
@file rtl/cadr_machine.sv
@note MEM<31:0> comes back from the bus interface to the processor, and this
@note is the only check that has both ends of it: `microcycle` is given the
@note word by its testbench and `memory_path` has no processor to give it to.
@note Rotated by a bit, every word the machine reads is plausible and wrong.
@note
@note Worth not re-deriving: crossing `-MEMACK` and `-LOADMD` here, which is
@note the wiring bug to reach for, is EQUIVALENT and provably so ---
@note `cadr_busint_xbus.sv` has `assign n_memack = !acked;` and `assign
@note n_loadmd = !acked;`, the same expression twice. They coincide on the
@note Xbus by construction, and only the Unibus tells them apart, where the
@note word comes before the acknowledgement. Until that path exists there is
@note no mutation of the crossing to write.
@note
@note IT SURVIVED ONCE AND DOES NOT NOW, and the record is kept for what
@note that says. It was `@hole #9` while the composed check stopped at the
@note wall, 537,845 microcycles and 515 bus cycles at the very end of the
@note run --- too few reads for a rotated word to reach a compared column.
@note With the machine writing its own mode register the check runs the
@note whole boot PROM, 17,466 bus cycles, and this is caught.
@note
@note So the hole closed by the surface growing, with nothing changed here
@note and nothing fixed: the prediction was that it would, and the run said
@note so by itself, as CLOSED, naming the issue and the line to delete.
@old
      .rdata       (rdata),
@new
      .rdata       ({rdata[30:0], rdata[31]}),
@end

@mutation the-mbusy-tap-is-charged-at-the-full-delay
@check microcycle
@file rtl/cadr_microcycle.sv
@note The other of the two countdowns off the acknowledgement, and the one
@note that had no mutation against it while `-RDFINISH` did. `MBUSY` clears
@note on `-MFINISHD`, the 30 ns tap of the TD50 at VCTL1 1D23 --- a number
@note read off the part, where RD_FINISH_T is a number chosen. Both deserve
@note interrogating and only one was.
@old
  localparam int unsigned MFINISHD_T  = 30 / 5;
@new
  localparam int unsigned MFINISHD_T  = 50 / 5;
@end

@mutation a-hang-may-be-taken-while-a-wait-is-up
@check microcycle
@file rtl/cadr_microcycle.sv
@note "A WAIT comes first, and this is not a tidiness: parking the generator
@note stops the master clock, and the master clock is what MBUSY.SYNC follows
@note MEMRQ on --- so a park taken while -WAIT is up would hold the cpu clock
@note for ever with nothing left to end it."
@note
@note That is a claim about the fabric hanging permanently, written in the
@note source and until now unbacked. If it is right this cannot merely
@note disagree with muir; it should fail to make progress at all, and the
@note testbench's own "never ran" assertion is what would say so.
@old
  assign hang = use_md && rd_in_progress && !wait_;
@new
  assign hang = use_md && rd_in_progress;
@end

@mutation a-tap-lands-at-the-wrap
@check phase_gen
@file rtl/cadr_phase_gen.sv
@note 5646c91 rewrote every tap from `phase_next == X` to `phase == X - 1`,
@note and its comment says why that is safe: "`phase_next` is only not
@note `phase + 1` at the wrap, and no tap lands there."
@note
@note That is a claim, not a measurement --- a sentence that makes the
@note rework correct, sitting in a comment. Its precondition is that no tap
@note lands at the wrap, so this puts one there: the control store's pulse
@note ends at `RESTART_T` from the read end instead of `WPIRAM_OFF_T`, which
@note is exactly `wrap_at`.
@note
@note WHAT IT PROVES AND WHAT IT DOES NOT. It proves the check notices a tap
@note moved onto the wrap, so the precondition is not one the fabric could
@note violate unseen. It does not prove the `- 1` transformation sound in
@note general, which is an argument about the code rather than about any
@note behaviour, and no mutation can reach it.
@old
        wpiram_off_at <= read_sel + 6'(WPIRAM_OFF_T) - 6'd1;
@new
        wpiram_off_at <= read_sel + 6'(RESTART_T) - 6'd1;
@end

@mutation the-word-is-corrupted-on-its-way-into-md
@check microcycle
@file rtl/cadr_microcycle.sv
@note MD is an OUTPUT of the processor now, not a column the testbench
@note drives, and it is loaded from the cables at -LOADMD. Nothing in this
@note list was aimed at it after it crossed over.
@note
@note That gap is worth naming rather than just filling. `md` stopped being
@note stimulus and became a thing to be checked; a9 found by hand that
@note turning a driven input into an output does not stop the testbench
@note driving it, and fixed the testbench. Nothing then proved the fixed
@note check bites. The file has seventeen other mutations, so my own
@note coverage warning --- which sees files and not signals --- called it
@note covered. Five more ports are about to make the same crossing.
@old
        md_held    <= rdata;
@new
        md_held    <= rdata ^ 32'd1;
@end

@mutation md-does-not-land-when-the-machine-is-hung
@check microcycle
@file rtl/cadr_microcycle.sv
@note The word is caught at -LOADMD and moved into MD at the next boundary
@note --- or at a hang, because a hung machine has no boundary coming and
@note the microcycle waiting on the word would never see it. Dropping the
@note hang term is the deadlock the two-stage hold exists to avoid, and it
@note is a claim of the same kind as -WAIT coming before -HANG.
@old
      end else if (md_pending && (mclk_edge || hang)) begin
@new
      end else if (md_pending && mclk_edge) begin
@end

@mutation the-address-the-decode-sees-is-a-microcycle-old
@check machine
@file rtl/cadr_microcycle.sv
@note `phys` is the live map output while MEMSTART is up and the registered
@note copy afterwards, and the live half is not a convenience. The grant is
@note taken at the edge that *ends* the MEMSTART microcycle, and the decode
@note the interface reads there --- Unibus or not --- has to be this cycle's
@note address. `phys_r` does not hold it until that same edge, so a decode
@note taken from `phys_r` alone is a microcycle behind at exactly the instant
@note it is read, and the Unibus cycle never enters ARB.
@note
@note That was a real bug and nothing in this list was aimed at it. It
@note matters more now than it did: `cadr_memory_path.sv` holds the decode in
@note a register, and a register on a signal is an invitation to move its
@note source back one more step.
@old
  assign phys = memstart ? {vmo[13:0], vma[7:0]} : phys_r;
@new
  assign phys = phys_r;
@end

@mutation the-held-decode-is-a-microcycle-late
@check machine
@file rtl/cadr_memory_path.sv
@note The decode is held for timing and the holding is a tick, which is
@note invisible because the address is constant for the microcycle it belongs
@note to. A microcycle is not invisible: the grant reads `unibus` at the edge
@note that ends the MEMSTART cycle, and `sel` is what tells main memory the
@note cycle is its. Clocking the register at the boundary instead of every
@note tick captures the address one microcycle after the cycle that used it.
@old
    end else begin
      is_memory <= is_memory_c;
@new
    end else if (mclk) begin
      is_memory <= is_memory_c;
@end

@mutation the-request-is-held-a-microcycle-rather-than-a-tick
@check machine
@file rtl/cadr_microcycle.sv
@note `MEMSTART AND VMAOK` is held for a tick so the map stops reaching the
@note bus interface's own counter, and the argument that a tick is safe is
@note that the interface reads -MEMRQ once, at the end of the microcycle,
@note and the request has been up since the map settled early in it. The
@note argument does not extend to a microcycle: sampling the term at the
@note boundary catches it before MEMSTART rises, so the request stands a
@note whole cycle late and so does the grant.
@note
@note This is the counterpart of `the-held-decode-is-a-microcycle-late`. Both
@note say the same thing about the same move --- a register put in for timing
@note is safe at one tick and wrong at one microcycle --- and both are here
@note because the difference between those two is invisible in the diff.
@old
      memgo_q <= memgo;
@new
      if (mclk_edge) memgo_q <= memgo;
@end

@mutation the-unibus-acknowledgement-and-the-md-strobe-change-places
@hole #13
@check machine
@file rtl/cadr_busint_xbus.sv
@note The two Unibus deadlines are held as instants rather than added to a
@note captured SSYN time, so that the adder is off the path that ends on the
@note processor's read-finish counters. Holding an instant is only the same
@note arithmetic if the sum is still made: dropping it acknowledges the
@note cycle where the MD strobe belongs and strobes MD where the
@note acknowledgement belongs, fifty nanoseconds apart the wrong way --- which
@note is exactly the distinction the comment above them draws: "the word
@note lands 50 ns *before* the acknowledgement, where an Xbus word lands
@note with it". Written as an exchange rather than as dropping a sum,
@note because dropping one leaves its parameter unread and lint rejects the
@note mutation before the check ever sees it --- twice, on the way to this.
@note
@note WHY IT SURVIVES, which is #13 and is not the same thing as the
@note `-XBUS.RQ` wrap. That was a signal no reference had a column for, and
@note the fix was to give a testbench somewhere to look. This is **a path
@note nothing enters**: `tb/cadr_busint_xbus_tb.cpp` never drives `unibus`,
@note so the whole ARB/UB branch is untested by construction rather than by
@note oversight, and no column would help. MIT's boot PROM makes exactly one
@note Unibus cycle and the composed check exempts arbitrated cycles from the
@note length comparison --- so the one cycle that exists is the one cycle
@note exempted. The band makes 347 and is not run composed.
@note
@note The record stays and stays red until that testbench drives `unibus`
@note and exercises arbitration. It is not an equivalence: exchanging the two
@note instants puts -LOADMD after -MEMACK instead of before, which is
@note observable wherever a Unibus read's word is used. Nothing here uses
@note one.
@note
@note The anchor follows the line: both instants are held one tick early now,
@note because the comparisons that read them are registers rather than gates
@note on the acknowledgement's path. Exchanging them is unchanged by that ---
@note the `- 1` is on both.
@old
            ub_ack_at   <= elapsed + 10'(UB_ACK_T) - 10'd1;
            ub_md_at    <= elapsed + 10'(UB_STROBE_T) - 10'd1;
@new
            ub_ack_at   <= elapsed + 10'(UB_STROBE_T) - 10'd1;
            ub_md_at    <= elapsed + 10'(UB_ACK_T) - 10'd1;
@end

@mutation the-word-at-the-xbus-seam-is-rotated
@check machine
@file rtl/cadr_machine.sv
@note `cadr_machine.sv` used to bring out `dev_rq`, `dev_write` and `phys`
@note and not the word, so a slave hung on this seam could be written to and
@note never see what --- and the failure would have appeared in the slave.
@note The port is new and this is what says it carries something.
@note
@note It is not covered by MD being compared every microcycle: `wdata` is a
@note register of its own, loaded from MD at the edge that starts the cycle,
@note so MD can be right on every one of 600,000 microcycles while the word
@note at the seam is wrong.
@note
@note IT IS A COMPLEMENT AND NOT A ROTATION, AND THAT IS THE INTERESTING
@note PART. All 5,650 device writes the boot PROM makes carry the same word
@note and that word is zero, so a rotation --- the obvious mutation, and the
@note one written first --- survives: a bijection on the bits of zero is
@note zero. It is an equivalence on this trace rather than in general, which
@note is the control store's all-zero pass again in a new place. The
@note complement makes a nonzero word out of a zero one and is caught. The
@note distinct-word count is on the check's own output, and the check says so
@note when it rises above one, because that is when the rotation becomes
@note live and this note becomes wrong.
@old
  assign dev_wdata  = wdata;
@new
  assign dev_wdata  = ~wdata;
@end

# ======================================================= the disk controller ===
#
# `rtl/cadr_disk_controller.sv`, the four registers at 0o17377774, held to
# `disk_controller::Controller`'s `status()`, `read` and `write` over MIT's
# boot PROM.
#
# **READ THIS BEFORE FILING A HOLE HERE.  ON THIS TRACE THE WHOLE MODULE
# REDUCES TO ONE CONSTANT AND A DECODE, SO A MUTATION THAT LEAVES `0x2321`
# ALONE IS AN EQUIVALENCE AND NOT A HOLE.**  The boot PROM makes 11,301 reads
# of the status register, every one answering `0x2321`, and 5,650 writes of
# ZERO to the disk address register.  It never writes a command, never touches
# CLP, never stores into START, and never reads back register 1, 2 or 3.  So:
#
#   the command register's contents      never written, so `cmd` is zero and
#                                        the two status bits it decides ---
#                                        <13> off CMD2 and <3> off the done
#                                        enable --- are constant here.  What
#                                        IS live is the DIRECTION each is
#                                        computed in, which
#                                        `disk-lossage-reads-cmd2-the-wrong-way`
#                                        is aimed at.
#   CLP                                  written by nothing on this trace
#   the DA register's storage            written 5,650 times and read never
#   the register select's low bit        registers 1, 2 and 3 are read by
#                                        nothing, so a rotation of `which`
#                                        moves a write from DA to CLP and a
#                                        read of nothing to a read of nothing
#   START                                stored into never
#   an acknowledgement a tick LATE ON A
#   WRITE                                invisible: 5,650 device writes
#                                        acknowledged a tick late is exactly
#                                        what this check passed with while the
#                                        testbench answered them, measured on
#                                        its own -MEMACK histogram
#   the address match held in a
#   register rather than combinational   a timing property and not a
#                                        functional one, so no simulation
#                                        check here can see it either way ---
#                                        and it is not free: combinational it
#                                        carries the map's ripple into
#                                        -MEMACK and the DDR=0 board flow
#                                        reads -6.195 ns on 1,065 endpoints
#                                        of 14,312 against +0.067 ns MET and
#                                        none failing with it held. What
#                                        holds that is the fitter, and the
#                                        module says so at the register.
#
# That is the control-store-writes-all-zeros shape for the third time in this
# repository, and the answer is the same one: the trace that tells a register
# block from a wire is the band, which needs the drive, the channel and
# `S_AXI_HP2`.  Until then the records here aim at what `0x2321` and its
# timing can see, and the list above is what they deliberately do not cover.

@mutation disk-not-on-cylinder-at-bit-7
@check machine
@file rtl/cadr_disk_controller.sv
@note `<8>` is "not on cylinder", `-ON CYL SYNC` never coming with nothing on
@note the cable, and `<7>` is the selected drive's read-only switch. Moved
@note one place down, the no-drive word becomes 0x22A1 where muir's
@note `status::NO_DRIVE` is 0x2321 --- `tests/disk.rs` asserts the octal
@note 0o21441 off both the behavioural model and the netlist board --- and
@note the boot PROM's first status poll takes it.
@note
@note THIS IS THE ONE THAT MATTERS, because it is the smallest change the
@note check can still see: the module's whole answer on this trace is that
@note one word, so a bit of it is the finest grain there is.
@old
                          : 32'h0000_0320)                     // <9> <8> <5>
@new
                          : 32'h0000_02a0)                     // <9> <7> <5>
@end

@mutation disk-lossage-reads-cmd2-the-wrong-way
@check machine
@file rtl/cadr_disk_controller.sv
@note `<13>` transfer aborted is `-LOSSAGE`, and with nothing on the cable
@note the disk lossage stands: `NO SELECT` through the 74LS32 at DCCMD 0C16,
@note which passes it only with CMD2 low. So the bit is the complement of
@note the command register's bit 2 and not the bit itself.
@note
@note The direction is what is mutated rather than the term, because the
@note term is constant on this trace --- the boot PROM never writes a command
@note --- and inverting it is what moves the answer to 0x0321.
@old
  assign disk_lossage     = cmd[2] ? 1'b0
@new
  assign disk_lossage     = !cmd[2] ? 1'b0
@end

@mutation disk-says-it-is-busy
@check machine
@file rtl/cadr_disk_controller.sv
@note `<0>` not active: "0 means the controller is busy, 1 means it is ready
@note to accept a command." `not_active()` is `now >= done_at`, which in
@note fabric is the busy counter at zero. The boot PROM starts nothing that
@note takes time, so the counter is zero throughout and inverting the test
@note makes the controller permanently busy.
@note
@note This is the bit the boot PROM BRANCHES on, and it is what the whole
@note slice buys: with it clear the machine waits for not-active and never
@note reaches the on-line wait at 0o541, which is the loop `rtl.golden`
@note spends its last 62,154 microcycles in.
@note
@note So it is caught on the branch and not only on MD. Measured: microcycle
@note 537,849 fails on the M bus (0x2320 for 0x2321), the ALU, R, OB, JCOND,
@note PCS1 and MD, and 537,850 on PC --- 0o541 where the reference says
@note 0o545. A no-drive controller that never answered at all fails the same
@note rows the same way, MD reading zero instead of 0x2320, which is what
@note the board did before this module existed.
@old
  assign not_active = (busy_ns == 32'd0);
@new
  assign not_active = (busy_ns != 32'd0);
@end

@mutation disk-decodes-one-word-low
@check machine
@file rtl/cadr_disk_controller.sv
@note The four registers are at 0o17377774-7 and this module decodes them
@note out of `phys` for itself, `sel` saying only that the cycle is some
@note device's. One word low and nothing answers 0o17377774 at all: the
@note cycle reaches no slave, the interface's own timer ends it 4.25 us
@note later, and MD takes zero where muir has 0x2321.
@note
@note Written as an XOR rather than as a different constant so that every bit
@note of `phys` stays read: CLAUDE.md's rule that lint catches a mutation at
@note bit granularity, and that where lint does the catching the check never
@note sees it.
@old
  assign mine_c  = sel && (phys[21:2] == REGS_PAGE);
@new
  assign mine_c  = sel && ((phys[21:2] ^ 20'd1) == REGS_PAGE);
@end

@mutation disk-answers-a-tick-late
@check machine
@file rtl/cadr_disk_controller.sv
@note muir's controller answers a device cycle in 0 ns of its own ---
@note `IDEAL_DEVICE_NS = 0` --- and `cadr_busint_xbus.sv` supplies the 80 ns
@note of bus setup and the 60 ns deskew of the TD100 at REQLM 0C09 itself.
@note So a slave that answers in the tick -XBUS.RQ reaches it puts -MEMACK at
@note muir's own instant, and a register anywhere between `dev_rq` and
@note `dev_ack` puts it 5 ns out on every cycle.
@note
@note A READ IS WHAT CATCHES THIS AND A WRITE IS NOT, which is worth stating
@note so the next person does not read the survivor list wrongly. A read's
@note acknowledgement ends a hang, and `RD_FINISH_T` counts from it, so a
@note tick moves the microcycle's length and the length check has no
@note tolerance. A write's does not: this same check passed for months with
@note all 5,650 device writes acknowledged a tick late by the testbench that
@note answered them.
@old
  // 0 ns of its own, and it must stay a gate: see the note at the top.
  assign dev_ack = asked;
@new
  // 0 ns of its own, and it must stay a gate: see the note at the top.
  logic ack_d;
  always_ff @(posedge clk) ack_d <= asked;
  assign dev_ack = asked && ack_d;
@end

@mutation rdcyc-gate-dropped
@check machine
@file rtl/cadr_microcycle.sv
@note "-LOADMD equals MEMACK and RDCYC". The interface asserts -LOADMD on
@note every acknowledgement, read or write --- `Busint` and
@note `cadr_busint_xbus.sv` both do --- and it is this gate, on the
@note processor's side where MIT put it, that keeps a write from strobing MD.
@note
@note IT IS HERE BECAUSE THE SLICE THAT ADDED THE DISK CONTROLLER COULD HAVE
@note TAKEN IT AWAY. What makes an extra load visible is the seam carrying
@note something MD must not take, and the testbench used to supply that by
@note answering device cycles with the complement of the word. Deleting that
@note line was the point of the slice; had `device_rdata` simply gone quiet,
@note a write would have loaded MD with zero, which is what MD already holds
@note on every device write this program makes, and the gate would have
@note become unobservable in silence. `tb/cadr_machine_tb.cpp` puts poison on
@note the seam on every microcycle instead, and `cadr_machine.sv` lets it
@note through on a write, where no slave inside drives MEM<31:0>.
@note
@note MEASURED BOTH WAYS, because "still caught" is not the same claim as
@note "caught by what I think": with the seam held at ZERO this is caught on
@note exactly 2 rows, 537,847 and 537,848, and both of them are the single
@note UNIBUS write --- `cadr_memory_path.sv` returns {16'hffff, ub_rdata}
@note there, which is the one word in this program a write puts on MEM<31:0>
@note by itself. A check standing on one bus cycle in 17,466 is one edit away
@note from standing on none. With the poison it is caught from the first
@note device write at 537,854 and on all 5,650 of them.
@note
@note Reasoning measured at the commit that added
@note `rtl/cadr_disk_controller.sv`.
@old
  assign loadmd_edge = !n_loadmd && n_loadmd_q && rdcyc;
@new
  assign loadmd_edge = !n_loadmd && n_loadmd_q;
@end

# ================================================== the machine, behind DDR ===
#
# `build/ddr_boot.pass` runs the same eight modules as the composed check
# against a different question.  `machine` asks whether the fabric agrees with
# muir; this asks what it does where muir cannot follow.  muir has a modelled
# disk controller that answers the boot PROM's status polls and the board has
# `device_ack` tied low and none at all, so past about microcycle 537,900 the
# two machines part company by design.  What stands in for a reference is the
# boot PROM's own PAGE-0-PARITY-FIX --- 512 bus cycles, a read and a write of
# each of physical 0..377, the write carrying the word the read returned ---
# poisoned from outside, injectively in the address, against a modelled DDR3
# that answers at a delay that varies per transaction.
#
# WHAT THIS CHECK CAN AND CANNOT SEE ABOUT A WRITE, said here once so that
# nobody files the gap twice.  The parity loop is an IDENTITY copy: every
# write carries the word already at that address.  So a bridge that wrote the
# wrong word is caught only when the wrong word is DISTINGUISHABLE from the
# right one --- the address mixed in, the halves swapped --- and a bridge that
# wrote the word it had just READ is not caught at all, because in this
# program they are the same word.  That is an equivalence on this program, not
# a hole, and it is the control store's all-zero pass one bus along.  A
# configuration that once broke it existed: with an unanswered read handing MD
# the bridge's stale word, a poison with bit 0 set in word 255 made the PROM
# write a CCW to physical 777.  **That behaviour was decided against** --- an
# unanswered read gives MD zero --- and configuration B of the testbench is
# now what asserts it never happens.  The witness went with it; the write path
# stands on step two of the board plan, which proved it on silicon.

@mutation the-bridge-holds-the-word-past-its-cycle
@check ddr_boot
@file rtl/cadr_xbus_ddr.sv
@note THE BUG THIS CHECK WAS BUILT AROUND, restored.  `rdata` is the slave's
@note driver onto MEM<31:0>; held across cycles it hands MD a word on every
@note cycle nothing answered, because `cadr_memory_path.sv` falls through to
@note `memory_rdata` and -LOADMD is asserted on the NXM timer's
@note acknowledgement too.  On the DDR board that word is the last word of
@note page 0 and bit 0 of it is what the boot PROM reads as a ready disk
@note controller, so what happened to be in main memory decided where the
@note machine went.
@note
@note Caught by configuration B and by nothing weaker, measured rather than
@note assumed: aimed in turn at `machine`, `memory_path` and `probe`, it
@note SURVIVES all three.  None of them ever leaves a word in this register
@note and then runs a cycle nothing answers --- `machine` answers all 16,951
@note device cycles from the trace, and `memory_path`'s stimulus never does
@note either.  The line that
@note fires first is "B: 513 DDR transactions, wanting 512", and the run also
@note reports the machine ending at PC 40, one word of the window not its
@note poison, and configuration B running 537,978 microcycles against A's
@note 592,681.
@old
      done  <= 1'b0;
      rdata <= 32'd0;
    end else if (mem_done && !done) begin
@new
      done  <= 1'b0;
    end else if (mem_done && !done) begin
@end

@mutation the-read-address-is-rotated
@check ddr_boot
@file rtl/cadr_xbus_ddr.sv
@note AT THE READ ONLY, and that is the whole of the record.  `mem_addr`
@note addresses this slave's write as well as its read, so a bijection
@note applied to both relabels the storage consistently --- written to the
@note wrong entry, read from the same wrong entry --- and nothing downstream
@note can tell.  CLAUDE.md records that as the second class of equivalent
@note mutant found in this project, one step from being filed as a hole
@note against a check doing its job.  Here the memory is the testbench's and
@note the address sequence is known from `promh.text` before the machine
@note runs, so the read alone is what has to move.
@note
@note Written as a rotation rather than a truncation so that every bit of
@note `phys` stays used and lint does not do the catching.
@old
  assign mem_addr  = main_byte_address(phys);
@new
  assign mem_addr  = main_byte_address(dev_write ? phys : {phys[20:0], phys[21]});
@end

@mutation the-write-never-reaches-the-memory
@check ddr_boot
@file rtl/cadr_xbus_ddr.sv
@note A write that never leaves the bridge.  `dev_write` stays read, through
@note `mem_write`, so lint has nothing to say about it.
@note
@note The cycle then ends on the NXM timer instead of on an answer, which is
@note what makes this visible at all: the WORD written cannot be checked
@note against an identity copy, but the transaction can be counted, and 256
@note of them stop happening.
@old
  assign mem_req   = asked && !done;
@new
  assign mem_req   = asked && !done && !dev_write;
@end

@mutation the-write-carries-the-address-mixed-into-the-word
@check ddr_boot
@file rtl/cadr_xbus_ddr.sv
@note The shadow-memory lesson at the far end of the path: a bridge writing
@note the address instead of the data.  Mixed in with an XOR rather than
@note replacing the word, so that `wdata` stays read at every bit --- lint
@note catches a signal nobody reads, and the catching belongs to the check.
@note
@note An identity copy is what makes this visible without a shadow memory at
@note all: the word written back must equal the word read, and the testbench
@note holds both.
@old
  assign mem_wdata = wdata;
@new
  assign mem_wdata = wdata ^ main_byte_address(phys);
@end

@mutation the-word-arrives-with-its-halves-swapped
@check ddr_boot
@file rtl/cadr_xbus_ddr.sv
@note The read data lane crossed.  Every bit of `mem_rdata` stays used, which
@note is the only way to write it: dropping half would leave the other half
@note unread and lint would reject the mutant before the check saw it.
@note
@note WHAT CATCHES IT IS THE IDENTITY, and by a route worth writing down.
@note The harness records what the MODEL returned, not what the bridge
@note latched, so the read is compared against its poison and agrees; what
@note disagrees is the write-back, which carries the swapped word.  Measured:
@note every one of the 256 fails on "word N was read as X and written back as
@note Y, so the copy is not the identity", and none on the read.  A harness
@note that had taken the read from the DUT would have swapped both ends and
@note seen nothing --- the shadow-memory lesson one move along.
@old
      if (!dev_write) rdata <= mem_rdata;
@new
      if (!dev_write) rdata <= {mem_rdata[15:0], mem_rdata[31:16]};
@end


# ================================================ the memory port's tally ===
#
# `rtl/cadr_mem_count.sv` is the board's only POSITIVE witness that the
# machine's memory cycles were answered, and everything below is aimed at it.
# The boot PROM's whole main-memory traffic is an identity copy of page 0, so
# page 0 reading back unchanged afterwards says the same thing whether the
# port answered or was never brought up; no lamp separates them either ---
# measured, LD2 reads the same with DDR and without, because the 16,951 disk
# polls time out either way. So step four of the board plan is read by four
# numbers on EMIO GPIO and by nothing else, which makes an unchecked
# instrument the worst kind of thing to have here: it will be read once and
# believed.
#
# `tb/cadr_mem_count_tb.cpp` runs the machine three times over the same
# adapter and widening the board has, and what each configuration is for
# decides which record below is caught by which:
#
#   LIVE   the port answers.  256 reads and 256 writes asked for, and 256 and
#          256 answered.  Anything that MIS-counts a handshake shows here.
#   DEAD   `hp0_aresetn` held low, which is a board before anybody has run
#          `ps7_post_config`.  The same 256 and 256 asked for, and NOTHING
#          answered.  Anything that counts the fabric's own intentions shows
#          here and only here.
#   HALF   the port takes a write and never answers it.  One read answered
#          and no writes --- the only reading in which the two directions
#          give different numbers, and so the only one that can see them
#          crossed.
#
# WHY THE THIRD CONFIGURATION EXISTS, since it is not a board anybody expects
# to meet: the boot PROM does exactly as many reads as writes, so a tally with
# its two directions crossed reads 256 and 256 in LIVE and 0 and 0 in DEAD and
# is invisible to both. `the-two-directions-of-the-tally-are-crossed` below is
# that mutation and HALF is what catches it. The configuration was written
# because the mutation survived, which is the order these things should
# happen in.
#
# WHAT CANNOT BE WRITTEN AS A RECORD HERE, so that nobody files a hole for it.
#
#   **A COUNTER OF `awvalid` INSTEAD OF `bvalid`.** The module is given the B
#   and R channels and nothing else of the port, so there is no request-side
#   port signal in it to count. That is the design and not an omission: the
#   two records that OR the machine's own request into an `answered` counter
#   are the same bug in the only form this module can express, and DEAD
#   catches them.
#
#   **DROPPING ONE TERM OF A HANDSHAKE** --- `bready` alone, `rready` alone,
#   `rlast` alone --- for two separate reasons, and it is worth keeping them
#   apart. Written the obvious way, by deleting the term, all three leave the
#   port unread and **Verilator does the catching**: `%Error: ... 'bready'
#   ... UNUSEDSIGNAL`, and the check never sees the mutation. Written so that
#   the signal stays read but is ignored --- `(bvalid && bready) || (bvalid
#   && !bready)` --- all three are EQUIVALENCES, measured: every number in
#   all three configurations is identical to a correct run's. That is not a
#   hole either. `cadr_axi_master` holds `bready` up from the tick it enters
#   WRESP, which is at least a tick before this slave can raise `bvalid`, so
#   `bvalid` is never asserted without `bready` beside it; the same argument
#   holds for `rready`; and at a burst of one every beat is the last, so
#   `rlast` never parts company with `rvalid`. The mutation that BITES is
#   dropping the slave's `valid` and counting the master's wait, which is
#   what the two `while-the-master-is-still-waiting` records below do.
#
#   **REMOVING THE SATURATION.** Nothing in this program reaches 32,767 of
#   anything, so a counter that wrapped would be identical here. The
#   saturation is for a machine that runs microcode, where a wrapped counter
#   reading a small number would be a false negative of exactly the kind this
#   module exists to rule out, and CLAUDE.md's `-XBUS.RQ` entry is the same
#   fact.
#
# AND THE PACKING IS MUTATED TOO, because it moved into this module from
# `rtl/cadr_arty.sv` for the reason the widening did: a field placed one bit
# over in a file nothing can simulate is a number that still looks like a
# measurement. The two records that reach it are the marker bits and the two
# halves changing places.

@mutation the-request-is-counted-as-a-write-answered
@check mem_count
@file rtl/cadr_mem_count.sv
@note THE FALSE WITNESS, which is the whole reason this module counts what it
@note counts. A tally that takes the fabric's own request as evidence that
@note the processing system answered reads 256 on a board whose port was
@note never brought up, and step four would be reported as passed.
@note
@note Written as an OR rather than as a replacement so that `bvalid` and
@note `bready` stay read: replacing the term outright leaves both unused and
@note Verilator does the catching, which is CLAUDE.md's rule about lint at
@note bit granularity. It is the same bug either way --- an event the fabric
@note generates by itself increments a counter of what came back.
@note
@note DEAD is what catches it, at "the tally counted 256 writes answered on a
@note port that answers nothing"; LIVE catches it too, at 512 for 256.
@old
      if (bvalid && bready && !(&answered_writes)) begin
@new
      if (((bvalid && bready) || (started && req_write))
          && !(&answered_writes)) begin
@end

@mutation the-request-is-counted-as-a-read-answered
@check mem_count
@file rtl/cadr_mem_count.sv
@note The read channel's half of the record above. The two counters are
@note separate code and a check that caught one and not the other would be
@note reported as green.
@old
      if (rvalid && rready && rlast && !(&answered_reads)) begin
@new
      if (((rvalid && rready && rlast) || (started && !req_write))
          && !(&answered_reads)) begin
@end

@mutation the-write-is-counted-while-the-master-is-still-waiting
@check mem_count
@file rtl/cadr_mem_count.sv
@note The handshake reduced to the master's own half of it. `bready` is a
@note level --- `cadr_axi_master` holds it up for the whole of WRESP --- so
@note this counts ticks of waiting rather than answers, and the number goes
@note up with the memory's latency rather than with its traffic.
@note
@note This is the "drops a term of the handshake" mutation in the only form
@note that is not an equivalence; the section header above says why dropping
@note `bready` alone is one.
@old
      if (bvalid && bready && !(&answered_writes)) begin
@new
      if ((bready || bvalid) && !(&answered_writes)) begin
@end

@mutation the-read-is-counted-while-the-master-is-still-waiting
@check mem_count
@file rtl/cadr_mem_count.sv
@note The read channel's half of the record above: `rready` is
@note `state == RDATA`, a level, and counting it counts the wait.
@old
      if (rvalid && rready && rlast && !(&answered_reads)) begin
@new
      if ((rready || (rvalid && rlast)) && !(&answered_reads)) begin
@end

@mutation the-request-is-counted-every-tick-it-stands
@check mem_count
@file rtl/cadr_mem_count.sv
@note `mem_req` IS A LEVEL, held up until the word is done or the interface
@note has timed the cycle out, so what makes `asked_*` a count of
@note transactions is the edge and nothing else. Inverted rather than
@note deleted so that both `req` and `req_q` stay read.
@note
@note A cycle that times out stands for about 850 ticks, so this does not
@note merely inflate the number --- it saturates both counters, and a
@note saturated tally is the one reading that cannot be told from a real one
@note by its shape alone. That is the argument for asserting the number and
@note not merely that it is non-zero.
@old
  assign started = req && !req_q;
@new
  assign started = req && req_q;
@end

@mutation the-marker-bits-say-nothing
@check mem_count
@file rtl/cadr_mem_count.sv
@note THE BIT PATTERN THAT SAYS WHO WROTE THE WORD, inverted. Measured on the
@note board: with the level shifters on and nothing in the fabric driving the
@note EMIO pins --- a DDR=0 bitstream --- the processing system reads both
@note registers as 0xFFFFFFFF, which is exactly what four saturated counters
@note read. `(w & 0x80008000) == 0x00008000` is what separates them, and with
@note the two constants swapped an all-ones reading satisfies it and a real
@note one does not.
@note
@note Swapped rather than deleted because the field widths have to stay 64
@note bits: a constant removed is a width error and the build does the
@note catching.
@old
  assign gpio = {1'b0, asked_writes,    1'b1, asked_reads,
                 1'b0, answered_writes, 1'b1, answered_reads};
@new
  assign gpio = {1'b1, asked_writes,    1'b0, asked_reads,
                 1'b1, answered_writes, 1'b0, answered_reads};
@end

@mutation the-two-halves-of-the-tally-change-places
@check mem_count
@file rtl/cadr_mem_count.sv
@note THE PACKING, and the reason it is in this module rather than in
@note `rtl/cadr_arty.sv` where it was written: a field placed in the wrong
@note register is a number that still looks like a measurement, and the top
@note level cannot be simulated. With the halves swapped, `DATA_2_RO` carries
@note what was asked for and `DATA_3_RO` what was answered --- and on a board
@note whose port is dead that reads as 256 answered and nothing asked, which
@note is the false witness by another route.
@old
  assign gpio = {1'b0, asked_writes,    1'b1, asked_reads,
                 1'b0, answered_writes, 1'b1, answered_reads};
@new
  assign gpio = {1'b0, answered_writes, 1'b1, answered_reads,
                 1'b0, asked_writes,    1'b1, asked_reads};
@end

@mutation the-two-directions-of-the-tally-are-crossed
@check mem_count
@file rtl/cadr_mem_count.sv
@note THE RECORD THE THIRD CONFIGURATION EXISTS FOR. The boot PROM reads
@note page 0 and writes it straight back, 256 of each, so a tally with its
@note two `answered` counters swapped reads 256 and 256 in LIVE and 0 and 0
@note in DEAD --- byte for byte what a correct one reads. Measured: with the
@note two swapped, LIVE prints 256 and 256 and DEAD prints 0 and 0, and only
@note HALF disagrees. HALF was written because of it.
@note
@note On a port that takes a write and never answers it the machine gets one
@note read and stops: a correct tally reads one read answered and no writes,
@note and this one reads the reverse. That is the only reading here in which
@note the two directions differ at all.
@old
      if (rvalid && rready && rlast && !(&answered_reads)) begin
        answered_reads <= answered_reads + 1;
      end
      if (bvalid && bready && !(&answered_writes)) begin
        answered_writes <= answered_writes + 1;
      end
@new
      if (bvalid && bready && !(&answered_reads)) begin
        answered_reads <= answered_reads + 1;
      end
      if (rvalid && rready && rlast && !(&answered_writes)) begin
        answered_writes <= answered_writes + 1;
      end
@end


# ======================================================== the top level ===
#
# `rtl/cadr_arty.sv` is the only file here that cannot be simulated ---
# Verilator has no `MMCME2_BASE` --- so `build/arty.pass` is lint alone,
# against the stubs in `tb/cadr_arty_stubs.sv`. What lint can hold is
# nevertheless the file's whole specification, which its own comment states:
# `witness` folds every output of `cadr_machine` "so none of them is dead",
# because a top level that left the datapath unconnected would synthesise to
# almost nothing and still write a perfectly good bitstream of an empty part.
# Forty-nine outputs, forty-nine connected, forty-nine folded.
#
# WHAT THIS CHECK CANNOT SEE, MEASURED AT d454d1c AND NOT FILED AS A HOLE.
# Only a signal the fold is the *sole* reader of can be dropped visibly:
# then it is unread and `-Wall` gives UNUSEDSIGNAL. Four of the forty-nine
# are read somewhere else as well --- `clock_edge` counts microcycles into
# `beat`, `promdisable` colours LD4, `timed_out` feeds `nxm_count`,
# `n_memack` clocks `bus_nxm` --- and dropping any one of those four from the
# fold was measured to leave lint entirely clean. Four mutations, four rc=0,
# no warning of any kind. So the rule "every output" is enforced by this
# check for forty-five of the forty-nine and by nothing at all for the other
# four; on the board they would still reach a pin through their LED, which is
# why the omission does not delete them and also why nothing complains. It is
# a real limit and it is written here rather than filed, because a record with
# an `@hole` needs an issue and inventing one to hold a limit that was never
# a defect is the suppression this list exists to prevent.

@mutation an-output-is-folded-twice-and-another-not-at-all
@check arty
@file rtl/cadr_arty.sv
@note The fold is forty-nine identifiers written by hand in one expression,
@note and the failure a list like that has is not a deleted line, it is a
@note name repeated where the next one belongs. `pc` for `lpc` costs two of
@note them at once: `lpc` stops being folded, and `pc` XOR `pc` is zero, so
@note `pc` stops contributing too. Synthesis would then be free to delete
@note whatever computes either.
@note
@note Caught because `lpc` has no other reader: UNUSEDSIGNAL at the
@note declaration, which is the only shape of this bug lint can see. The
@note section note above says which four outputs are outside that.
@old
      witness <= ^{pc, lpc, opc, st, ir, a, m, alu, r, ob, q, dc, lc,
@new
      witness <= ^{pc, pc, opc, st, ir, a, m, alu, r, ob, q, dc, lc,
@end

@mutation the-word-at-the-xbus-seam-reaches-no-pin
@check arty
@file rtl/cadr_arty.sv
@note The bug d454d1c found, put back. `dev_wdata` is MEM<31:0> on its way to
@note an Xbus slave; no slave exists, so nothing in the top level reads it and
@note it was connected to nothing at all --- the one output of `cadr_machine`
@note that reached neither the instantiation nor the fold. Left that way,
@note synthesis deletes the register the word comes out of and the seam the
@note disk controller will hang on is a pin count and not a path.
@note
@note Two warnings rather than one, and they are the two halves of the claim:
@note PINMISSING says the port list is incomplete, UNDRIVEN says the fold is
@note reading something the machine no longer supplies. The fold is what
@note turns the first into the second, which is the argument for having it.
@old
      .dev_write(dev_write), .dev_wdata(dev_wdata),
@new
      .dev_write(dev_write),
@end

@mutation the-beat-that-came-back-is-the-one-that-went-out
@check arty
@file rtl/cadr_arty.sv
@note The `DDR` arm is a branch only one build reaches. Until this record the
@note runner linted the default board alone, where `g_ddr` is not elaborated,
@note and nothing in this list was aimed at anything inside it --- so the
@note wiring between `cadr_axi_master`, `cadr_axi_widen` and `cadr_ps7` had no
@note check of any kind. It lints all three configurations now, the same three
@note `build/arty.pass` runs.
@note
@note The bug is the read path crossed onto the write path: what the widening
@note is handed as the beat that came back is the beat that went out, so every
@note main-memory read returns the last word written rather than the word at
@note the address. A machine that boots and reads back garbage.
@note
@note MEASURED, AND IT IS THE WHOLE ARGUMENT FOR THE THREE PASSES. The default
@note lint says nothing at all about this: `hp0_rdata` is unread only in a
@note branch that configuration does not elaborate, so it exits 0. So does the
@note probe board's. Only the `DDR=1` pass reports it, as UNUSEDSIGNAL on the
@note declaration, and that is the one the runner did not run.
@note
@note And what lint still cannot see is worth writing down beside it: a
@note crossing that leaves every signal read --- `.m_awaddr(hp0_araddr)` with
@note `.m_araddr(hp0_awaddr)` --- draws no warning from anything. This check
@note holds that the widening is wired to something; `axi_widen`'s own
@note testbench is what holds the conversion itself.
@old
        .m_rdata(hp0_rdata)
@new
        .m_rdata(hp0_wdata)
@end

# ================================================================ the witness ===
#
# `rtl/cadr_prove.sv` is what goes on the board ahead of the machine, and it is
# the first thing here whose OTHER half is not fabric at all: a debugger
# reading and writing DDR from outside the design.  That is the whole reason
# the two steps exist --- CLAUDE.md's shadow-memory lesson says a model taken
# from the DUT moves with the bug, and an observer on the far side of the
# processing system cannot move at all --- and it is also why the check here
# has to bite.  A witness that is quietly wrong reports a memory fault that
# is its own.
#
# WHAT THE CHECK IS.  `tb/cadr_prove_tb.cpp` runs the harness --- the state
# machine with `cadr_axi_master` and `cadr_axi_widen` under it, wired as
# `rtl/cadr_arty.sv`'s `g_ddr` wires them --- against a 64-bit AXI3 slave whose
# model memory is filled with the COMPLEMENT of the word before every
# transaction.  The beat address, the strobes and the strobed half of the data
# are each compared against what the stimulus asked for and never against what
# the DUT presented; the neighbour in the beat is required to be untouched; and
# the address sweeps bit 2 and every bit above the beat boundary.
#
# AND THE INPUTS GO BAD MID-TRANSACTION, which is the one thing the board can
# never test.  `addr`, `word` and `writes` are tied to constants there, so a
# witness that read them instead of its own registers would be right by
# accident for ever.  Here they are scribbled with the complement and the
# neighbouring lane the moment the payload is taken.
#
# WHAT NOTHING HERE CAN CATCH, and it is worth saying rather than filing.
# `rtl/cadr_arty.sv` chooses the address and the word, and a top level that
# crossed `.addr(PROVE_ADDR)` with `.word(PROVE_WORD)` would leave every signal
# read, lint clean, and this check untouched --- the check cannot see the top
# level, and the top level cannot be simulated.  What stands against it is that
# both are localparams a person can read beside the reasoning for them, and
# that the debugger reads the address the file names.  It is the same limit
# `build/arty.pass` already has and not a new one.

@mutation the-request-goes-up-before-the-address-is-good
@check prove
@file rtl/cadr_prove.sv
@note The bus specification makes the master responsible for asserting good
@note address, write and data lines 80 ns before the request ---
@note `cadr_busint_xbus.sv`'s SETUP_T, sixteen ticks --- and
@note `rtl/cadr_ddr.xdc` relaxes the adapter's address and data registers to
@note sixteen ticks ON THE STRENGTH OF IT. So a witness that raises `mem_req`
@note early does not merely break a rule: it turns that constraint into a
@note claim about a board where it is false, and the fitter would time the
@note address path against 80 ns it does not have.
@note
@note Divided rather than replaced, so `SETUP_T` stays read and lint has
@note nothing to say. Four ticks is 20 ns.
@note
@note THE COUNTDOWN IS LOADED IN TWO PLACES SINCE THE WRITE-BACK ARRIVED ---
@note once for the first transaction and once for the second --- so the line
@note above it is carried here to say which. The other one is the record
@note below. A record that named neither would match twice and the runner
@note would say so, which is the good kind of rot.
@old
            read_agreed <= 1'b1;
            count       <= CW'(SETUP_T);
@new
            read_agreed <= 1'b1;
            count       <= CW'(SETUP_T / 4);
@end

@mutation the-write-back-does-not-wait-the-eighty-nanoseconds
@check prove
@file rtl/cadr_prove.sv
@note The second half of the record above. The write-back is a transaction of
@note its own and owes the bus the same sixteen ticks the first one did; the
@note easy mistake is to treat it as a continuation and send it straight out,
@note which is what this does. `rtl/cadr_ddr.xdc` would then be relaxing the
@note adapter's address registers for a payload that had not settled --- and
@note on a `PROVE=2` board the write-back is the ONLY transaction whose word
@note is not a constant the fitter can propagate, so it is the one that
@note actually needs the sixteen ticks.
@old
              echo_due    <= 1'b0;
              // The write-back is a transaction of its own and owes the bus
              // the same 80 ns the first one did.
              count       <= CW'(SETUP_T);
@new
              echo_due    <= 1'b0;
              count       <= CW'(SETUP_T / 4);
@end

@mutation a-refused-transaction-reads-as-a-good-one
@check prove
@file rtl/cadr_prove.sv
@note SLVERR or DECERR is a cycle that reached the port and was REFUSED,
@note which is a different fault from a wrong word and must not light the
@note same lamp. Inverting the term rather than deleting it, because deleting
@note leaves `mem_error` unread and Verilator rejects the mutation instead of
@note the check catching it --- CLAUDE.md's bit-granularity lesson.
@note
@note On the board this is the worst kind of wrong: step two would show green
@note for a write the memory controller never accepted, and the next step
@note would go looking for the fault in the machine. Written at the sequence's
@note last completion, which is the write on a write board and the write-back
@note on a read board; the read's own refusal arrives here through
@note `read_agreed` and the record below holds that half.
@old
              matched <= read_agreed && !mem_error;
@new
              matched <= read_agreed || !mem_error;
@end

@mutation the-verdict-is-taken-from-the-word-the-inputs-hold-now
@check prove
@file rtl/cadr_prove.sv
@note `want` is the word this sequence is about, held from the tick it
@note started. Comparing against the input instead is invisible on the board
@note --- `rtl/cadr_arty.sv` ties `word` to a constant, so the two are the
@note same signal there --- and it is exactly the class of bug that only a
@note check which moves its inputs can find. The testbench scribbles the
@note complement over `word` the moment the payload is taken, for this.
@note
@note WRITTEN AS AN XOR AND NOT AS A SUBSTITUTION, and the first draft was the
@note substitution. `want` is read nowhere else, so replacing it left the
@note register unused and Verilator rejected the mutation --- BROKEN, which is
@note a verdict on the record and never on the check. Mixed in instead, both
@note stay read and the verdict still depends on an input it must not: with
@note the testbench scribbling the complement over `word`, `want ^ word` is
@note all ones and no read can match. CLAUDE.md's bit-granularity lesson, met
@note for the third time in this list.
@note
@note It moved from `matched` to `read_agreed` when the write-back arrived,
@note because the read is no longer the last thing that happens in a sequence
@note and the read's opinion of the word is recorded where the read finishes.
@old
              read_agreed <= !mem_error && (mem_rdata == want);
@new
              read_agreed <= !mem_error && (mem_rdata == (want ^ word));
@end

@mutation the-witness-does-not-stop
@check prove
@file rtl/cadr_prove.sv
@note Both steps' whole requirement is ONE sequence and then silence: the port
@note comes live, the transactions go out, and somebody reads the result later
@note with nobody at the board. `go` is tied high there, so a HELD state that
@note returns to IDLE without waiting for it to fall runs the sequence over
@note and over for as long as the board is powered --- which reads on the
@note lamps exactly like the correct design and is a machine hammering the
@note memory controller a debugger is trying to read. On a `PROVE=2` board it
@note is worse than noise: the write-back would keep landing while the
@note debugger read it, so a value read out could be from any run.
@note
@note `go` stays read in IDLE, so lint has nothing to say.
@old
        HELD: begin
          if (!go) state <= IDLE;
        end
@new
        HELD: begin
          state <= IDLE;
        end
@end

@mutation the-word-lands-one-lane-over
@check prove
@file rtl/cadr_prove.sv
@note Bit 2 of the address is what `cadr_axi_widen.sv` places the write
@note strobes by and selects the read lane by, so flipping it moves the word
@note into the other half of the same 64-bit beat: the word is destroyed at
@note the address the debugger will read and written where its NEIGHBOUR
@note should be. Legal AXI, a clean OKAY, and a memory that disagrees with
@note the design about where it put things.
@note
@note It is the mutation the neighbour check exists for. A testbench that only
@note read back the address it wrote would follow the word into the wrong
@note lane and call it a pass --- which is CLAUDE.md's shadow memory, in the
@note one place in this module where it could happen.
@old
            mem_addr    <= addr;
@new
            mem_addr    <= addr ^ 32'd4;
@end

@mutation the-write-back-carries-the-constant-and-not-the-word-that-came-back
@check prove
@file rtl/cadr_prove.sv
@note THE POINT OF STEP THREE IS THAT THE FABRIC DOES NOT DO THE COMPARING.
@note A witness that writes out its own `want` instead of what the memory
@note returned reports the answer it was built with, whatever the port said
@note --- and on the board that is indistinguishable from a working read: the
@note debugger finds the word it wanted at the second address in all three
@note cases, including the two that were meant to come back wrong. It is the
@note lamp's own failure mode with the lamp taken away, which is why the
@note write-back carries the word RAW.
@note
@note `mem_rdata` stays read in the line above, so lint has nothing to say.
@note Caught by the wrong-word case and by the wrong-half case, which are the
@note two the board session runs first for the same reason.
@old
              mem_wdata   <= mem_rdata;
@new
              mem_wdata   <= want;
@end

@mutation the-write-back-lands-in-the-beat-it-just-read
@check prove
@file rtl/cadr_prove.sv
@note The second address must not share a 64-bit beat with the first, or the
@note write-back overwrites the thing it read --- and then a second run of the
@note same case reads what the first run wrote, which is a witness marking its
@note own homework by a longer route. `rtl/cadr_arty.sv` puts them seven beats
@note apart and says so; this puts them in the same one.
@note
@note Written as an XOR and not as the splice it is. The plain
@note `{mem_addr[31:3], echo[2:0]}` says exactly what this means and leaves
@note `echo[31:3]` unread, which Verilator rejects --- CLAUDE.md's
@note bit-granularity lesson for the fourth time in this list, and the fourth
@note time it has cost a draft. The XOR is the same value with every bit of
@note `echo` still read: the high bits cancel and the low three come through.
@note
@note Caught two ways --- the write-back's `awaddr` is not the second
@note address's beat, and the beat the read came out of no longer holds what
@note the stimulus seeded.
@old
              mem_addr    <= echo;
@new
              mem_addr    <= echo ^ {mem_addr[31:3] ^ echo[31:3], 3'b000};
@end

@mutation a-write-board-owes-one-transaction-and-a-read-board-two
@check prove
@file rtl/cadr_prove.sv
@note `echo_due` is the whole of the difference between the two steps: a read
@note owes a write-back and a write does not. Setting it unconditionally gives
@note a `PROVE=1` board a second transaction it was never meant to have ---
@note one that writes whatever `mem_rdata` happens to hold, which after a
@note write is zero, into the second address. Step two's board would then
@note write two words, and the one nobody expected lands in DDR the debugger
@note is about to read.
@note
@note `writes` stays read two lines above, so lint has nothing to say. Caught
@note on the handshake counts --- a write board owes exactly one AW --- and on
@note the second address's beat, which a write board must leave alone.
@old
            echo_due    <= !writes;
@new
            echo_due    <= 1'b1;
@end


# --------------------------------------------------- the probe, three of them
#
# `rtl/cadr_probe.sv` is the instrument the board will be read through, and
# it is the one piece of fabric here whose other verification --- a run on
# silicon --- has not happened. So the check on it has to bite, and these say
# whether it does. The check is `tb/cadr_probe_tb.cpp`: the machine runs from
# reset, 1,024 samples are shifted out through the probe's own JTAG shift
# register, and every column of every one of them is compared against
# `build/rtl.golden`.
#
# WHAT IS NOT HERE, and why. The mutation that matters most --- dropping the
# tick of holding, so the sample is the microcycle *after* the one it claims
# --- cannot be written as a record. Removing the hold leaves `data` unread
# and Verilator rejects it, which is the bit-granularity lesson in CLAUDE.md
# again: lint catches the mutation instead of the check. It was measured by
# hand instead, by deleting the register and its assignment together, and it
# fails at microcycle 3 on `pc`, `ir`, `nop` and `lpc` --- 21 mismatches in
# 110 comparisons. The two below reach the same property from the sides lint
# will allow.

@mutation the-sample-carries-the-next-microcycles-number
@check probe
@file rtl/cadr_probe.sv
@note `cycle` is the trace's first column and the only direct proof that the
@note window begins at microcycle zero: the comparison tool checks it counts
@note 0, 1, 2 with no gap and no repeat, and reports a capture without one as
@note resting on content alone. Storing the post-increment value puts every
@note sample under the number of the microcycle after it --- a whole-capture
@note offset of one, which is exactly what a dropped sample or a late arm
@note would look like, so a check blind to this is blind to those too.
@note
@note `cycle` stays read and stays written, so lint has nothing to say and
@note the check has to be the thing that catches it.
@old
        mem[wr_addr] <= {1'b1, cycle, data_q};
@new
        mem[wr_addr] <= {1'b1, cycle + 32'd1, data_q};
@end

@mutation the-readout-is-rotated-by-a-bit
@check probe
@file rtl/cadr_probe.sv
@note The half of this core that no simulation of the machine reaches and no
@note board has yet run: the JTAG shift register. Loading it a bit out of
@note place moves every field of every sample by one, which is not obviously
@note wrong to look at --- a 454-bit word of mostly zeros still looks like a
@note 454-bit word of mostly zeros --- and is wrong in every column.
@note
@note This is the mutation that says the testbench's model of a TAP is doing
@note work. Without the shift-out being part of the check, a core could store
@note the right samples and hand back nothing anybody could read, and every
@note check in the repository would still be green.
@note
@note A ROTATION AND NOT A SHIFT, and the reason is the bit-granularity
@note lesson: `sr <= {sr[SAMPLE-2:0], jtag_tdi}`, the obvious way to write
@note "shifts the wrong way", leaves `sr[453]` with no reader and Verilator
@note rejects it --- lint catches the mutation instead of the check. Rotating
@note the loaded word keeps every bit of `mem_q` used and leaves the catching
@note where it belongs.
@old
        sr      <= mem_q;
@new
        sr      <= {mem_q[0], mem_q[SAMPLE-1:1]};
@end

@mutation the-read-pointer-moves-on-every-bit
@check probe
@file rtl/cadr_probe.sv
@note The pointer advances on the CAPTURE edge, once a scan, deliberately:
@note that way a scan cut short or shifted with the wrong length still moves
@note it exactly once and the reader stays in step. Advancing it on each
@note shifted bit instead walks it 454 entries a sample, which the buffer's
@note wrap turns into a plausible-looking readout of the wrong rows rather
@note than into an error.
@old
      end else if (jtag_shift) begin
        sr <= {jtag_tdi, sr[SAMPLE-1:1]};
@new
      end else if (jtag_shift) begin
        rd_addr <= rd_addr + ADDR'(1);
        sr <= {jtag_tdi, sr[SAMPLE-1:1]};
@end

# -------------------------------------- the probe's reader, four of them
#
# `vivado/probe.tcl` is the other half of the probe: the thing that reads the
# capture off the board over JTAG and writes what `tools/probe_check.py`
# diffs against muir. Everything above this line is fabric; these four are
# not, and they are here because a script nobody can run is the sharpest
# version of the problem this list exists for.
#
# IT SHIPPED WITH A ONE-CHARACTER BUG AND NOTHING COULD HAVE CAUGHT IT. The
# IDCODE scan drove TDI with zeros, so running off the end of the chain
# returned more zeros --- indistinguishable from bypass bits --- and 192 bits
# of padding were counted as 192 devices. The IR scans that followed were 778
# bits where the chain wants 10, an over-long IR scan keeps only its LAST
# bits, and those were the all-ones BYPASS padding: USER1 was never selected
# in anything and every sample read back as a bypass bit. It survived because
# exercising it took a board.
#
# The check is `tb/cadr_probe_jtag_tb.tcl` against `tb/cadr_jtag_chain.tcl`:
# seven chains, `tclsh`, no Vivado and no board, 80 ms. It asserts for each
# case the LINE the script must print and not merely its exit code --- which
# is what makes `the-instruction-register-is-measured-and-ignored` catchable
# at all. A name rather than a position: the list grows and `the third of
# these` would quietly come to mean something else.
#
# WHAT THE CHECK CANNOT SEE is in `tb/cadr_jtag_chain.tcl`'s header and is
# the thing to read before trusting a green run: it is a shift chain, not a
# TAP state machine and not silicon, so a DRCK edge, `BSCANE2`'s real
# behaviour and every property of `rtl/cadr_probe.sv` are outside it. Those
# belong to the `probe` mutations above.
#
# THE REASONING BELOW WAS TRUE AT ace4131, which is where `vivado/probe.tcl`
# reached its present shape and where the chain was measured on silicon: both
# IDCODEs exact, the part at the TDO end, IR total 10, first bit out least
# significant, DONE high. CLAUDE.md's rule is that `@old`/`@new` rots loudly
# and prose rots silently, so the commit is named rather than the reasoning
# left to read as true for ever.
#
# ONE THING IS NOT HERE. Restoring the IR-padding sweep the fix deleted is
# the obvious extra record and is not written: the sweep would find a
# working padding on every chain the model presents that has one, and pass.
# What the sweep really cost was the ability to say WHICH padding was right
# --- and that is what `the-instruction-register-is-measured-and-ignored`
# below tests directly, from the side the check can see.

@mutation the-idcode-scan-drives-tdi-with-zeros
@check probe_jtag
@file vivado/probe.tcl
@note THE BUG THAT SHIPPED, restored exactly. What terminates the chain is
@note TDI coming back out behind the last device, so the scan is
@note self-terminating only if TDI is driven with something that is neither a
@note legal IDCODE nor a bypass bit. All ones is both. All zeros is neither:
@note a zero reads as a bypass device, and 192 of them read as 192 devices.
@note
@note Caught on five of the seven cases, which is itself the point --- the
@note failure is not subtle once anything at all can run the script. `good`,
@note `reversed` and `threedev` stop at "bit 64 of the IDCODE scan is a
@note bypass bit"; `msb-first` and `irlen` reach their own checks by a
@note different route and so fail on the wrong line, which the harness reads
@note as a failure for the right reason.
@note
@note `tdi-zero` and `stranger` are unmoved, and that is correct: `tdi-zero`
@note is the case that makes the board behave this way whatever the script
@note drove, so it fails identically either way.
@old
set idraw [scan_dr_hw_jtag $scan_len -tdi [string repeat f [expr {$scan_len / 4}]]]
@new
set idraw [scan_dr_hw_jtag $scan_len -tdi [string repeat 0 [expr {$scan_len / 4}]]]
@end

@mutation the-chain-is-read-from-the-wrong-end
@check probe_jtag
@file vivado/probe.tcl
@note `chain` is ordered from the TDO end, so the devices between ours and
@note TDO put their bypass bits IN FRONT of the sample in a DR scan and those
@note behind it put theirs after. Swap the two counts and the returned scan
@note is unpacked from the wrong offset --- the bit order of what comes back,
@note read backwards.
@note
@note It is worth having as a separate record from the one above because it
@note is the arithmetic no simulation of the fabric can reach: the probe core
@note never sees a chain, and `tb/cadr_probe_tb.cpp` shifts its samples out
@note with nothing either side of them. This is the only check anywhere that
@note holds the "N from the TDO end" line to anything.
@note
@note Caught on `good`, `reversed` and `threedev`, each at "the first sample
@note reads back valid=0" --- the sample is real and is extracted one bit out,
@note so the valid bit read is a neighbour's bypass bit. The four failing
@note cases never reach a sample and are unaffected, which is right: they are
@note about the chain and this is about what is done with it.
@old
set after  $mine
set before [expr {[llength $chain] - $mine - 1}]
@new
set after  [expr {[llength $chain] - $mine - 1}]
set before $mine
@end

@mutation the-instruction-register-is-measured-and-ignored
@check probe_jtag
@file vivado/probe.tcl
@note THE SWEEP'S SIN, WRITTEN AS ONE LINE. ace4131 replaced a search over IR
@note paddings with a single scan that MEASURES the chain's instruction
@note register length, the device order and Vivado's bit order at once, before
@note anything depends on any of them. This takes that scan, prints it, and
@note stops acting on it --- which is precisely what the sweep amounted to:
@note the measurement made, and nothing refused on it.
@note
@note IT DOES NOT MAKE A BAD CHAIN PASS. That is why it is here. With the
@note guard vacuous, `irlen` --- a part whose instruction register is five
@note bits where the BSDL says six --- goes on to select USER1 at offsets
@note that fit no device, reads 455 bits of nothing, and dies at "the chain
@note and the instruction register are the ones the BSDLs document and USER1
@note is selected, and the first sample still reads back valid=0", having
@note printed "captures 221, wanting 041" two lines earlier and done nothing
@note about it. The exit code is 1 either way.
@note
@note So a harness that checked exit codes would call this caught and be
@note wrong, and one that checked nothing but `good` would call it a
@note survivor. It is caught because every case names the line it must fail
@note on, holding the script to its own header's claim that "a board that is
@note not that chain fails on the check that names it, not on a sample of
@note zeros". That sentence is the check.
@old
if {($ircap & $cap_mask) != $cap_expect} {
@new
if {0 && ($ircap & $cap_mask) != $cap_expect} {
@end

@mutation the-done-bit-is-read-from-a-fixed-offset
@check probe_jtag
@file vivado/probe.tcl
@note DONE comes back for free on the way past --- bit 5 of the PL TAP's own
@note instruction-register capture, "1 when DONE is released" per its BSDL ---
@note and it is the only reading of DONE anywhere that comes from the TAP
@note rather than from what the programmer claimed. Its offset is
@note `our_ir_lsb + 5`, and `our_ir_lsb` is where the part's field begins,
@note which is 0 with the part at the TDO end and 4 with the DAP in front of
@note it.
@note
@note Drop the base and the read is right on the chain the board has and
@note wrong on any other: it takes a bit out of the neighbouring device's
@note capture field, which is a perfectly plausible 0 or 1 and changes no
@note exit code. Caught by `reversed` alone, and by nothing else, because
@note `reversed` is the only case with a device in front of the part --- which
@note is exactly why that case exists.
@note
@note Reasoning true at ace4131, where the DAP-before-part order was
@note confirmed on silicon and the BSDL's DESIGN_WARNING was read.
@old
 [bits $ircap [expr {$our_ir_lsb + 5}] 1]"
@new
 [bits $ircap 5 1]"
@end


# ================================ the disk controller, with a drive on it ===
#
# `rtl/cadr_disk_controller.sv` again, and the other half of it.  The section
# above is what MIT's boot PROM can see --- one constant and a decode --- and
# these are what only a drive can: the eight unit slots, the spindle's block
# counter, the seek and its attention, the hang timer, and the two resets.
# The check is `build/disk.pass` against `golden/src/disk.rs`'s program, which
# reaches all sixteen command codes and every reachable status bit where the
# band reaches six codes and no error bit at all.
#
# **A MUTATION AIMED HERE COSTS A MINUTE**, because the trace holds one hang
# run out to `TIMEOUT_NS` and the fabric counts all 512,000,000 ticks of it.
# That is the price of a check that can tell that constant from a wrong one.
#
# **TWO EQUIVALENCES, BOTH MEASURED, SO THAT NOBODY FILES THEM AS HOLES.**
#
# muir's `reset_errors` clears the timeout flop only when the controller is
# NOT ACTIVE; the board clears it unconditionally on `-RESET ERR` and lets the
# running timer set it again at expiry.  This module takes the board's, and
# `if (not_active) timeout <= 1'b0;` in its place SURVIVES `disk`, `machine`,
# `ddr_boot` and `probe` --- measured, not reasoned.  It cannot be told apart:
# muir gates both `<11>` and the transfer lossage with not-active anyway, and
# the board's flop is not set until the timer has run out, which IS not-active.
# CLAUDE.md named it as a candidate before this slice was written.
#
# And `-RESET ERR` clearing BUSY as well as the error flops also survives, for
# a reason about the PROGRAM rather than about the structure: every store into
# the command register that the trace makes while the sequencer is hung
# carries code 0o16, which stops the channel and clears BUSY on purpose.  So
# the trace never asks whether a store that is NOT a reset leaves a hang
# running.  It is the control-store-writes-all-zeros shape once more --- the
# stimulus does not reach the distinction --- and the live record aimed at the
# same line is `disk-reset-err-reaches-the-drive`, which asks the other half
# of the question and is caught.
#
# WHAT THIS CHECK DELIBERATELY DOES NOT COVER, so that nobody files a hole
# against it: everything a transfer leaves behind.  This slice has no channel,
# so the last memory address, the ECC register, the eight channel status bits
# and the heads' position after a command list are exempt on the rows a
# data-moving command touches --- bounded and counted on the check's own
# output, which says how many rows each cost.  Records for those arrive with
# the channel.

@mutation disk-selects-the-unit-from-the-wrong-bits
@check disk
@file rtl/cadr_disk_controller.sv
@note "Many bits in these registers refer to the 'selected unit', which is
@note that disk unit whose number is currently in bits <30:28> of the
@note disk-address register."  One bit down and the drive on unit 0 is found
@note by an address that names unit 0 and by three others that do not, and
@note lost by the one that does.
@note
@note **THIS IS THE MUTATION THE MODEL-FIRST METHOD FOUND BEFORE THE FABRIC
@note EXISTED.**  A Python model with one `present` flag rather than eight
@note slots read `1` where muir reads `0x2321` the moment the program stored
@note another unit number, and the program does that in its first bus cycle
@note --- unit 7, which has no drive and never will.
@old
  assign sel_unit  = da[30:28];
@new
  assign sel_unit  = da[29:27];
@end

@mutation disk-block-counter-off-by-a-region
@check disk
@file rtl/cadr_disk_controller.sv
@note "The count steps to `k` as region `k`'s pulse ends and holds the region
@note before it through the pulse."  Stepping at the region's START instead
@note leaves the counter one region ahead for the width of every pulse ---
@note 4,000 ns at the index and 1,240 elsewhere, out of regions of 968,448.
@note
@note So it is invisible except on the eighteen trailing edges, which is
@note exactly what the trace samples either side of: `grid_before(ends)` and
@note `grid_at(ends)` are one 5 ns tick apart and the testbench places each
@note on its own instant.  `DCHECK-BLOCK-COUNTER` reads the counter for half
@note a second and wants every value 0 to 17 and no other.
@old
    else block_counter = {3'd0, region - 5'd1};
@new
    else block_counter = {3'd0, region};
@end

@mutation disk-timeout-a-tick-short
@check disk
@file rtl/cadr_disk_controller.sv
@note `TIMEOUT_NS` is 2.56 s: the 74LS124 at DCTMOT 0B04 section 1 at 20 ms,
@note divided by 128 by the 74393 at 0C03.  That is 512,000,000 ticks of this
@note fabric's clock, and five nanoseconds off it is one tick.
@note
@note **THIS IS THE MUTATION THAT SAYS THE CHECK CAN SEE THE CONSTANT AT
@note ALL**, which is the `RD_FINISH_T` lesson: a correct derivation with a
@note blind check still drifts.  The trace reads the status register at
@note `hung_at + TIMEOUT_NS - 5` and again at `hung_at + TIMEOUT_NS`, and the
@note whole reason `build/disk.pass` takes a minute is that the fabric has to
@note count every tick between the hang and those two rows.  It is also the
@note reason the testbench anchors a group of rows on its last START rather
@note than on its first row: the hang is the twenty-seventh bus cycle at
@note instant zero, and a group placed forward from that instant starts the
@note timer 26 ticks late.
@old
  localparam logic [31:0] TIMEOUT_NS = 32'd2_560_000_000;
@new
  localparam logic [31:0] TIMEOUT_NS = 32'd2_559_999_995;
@end

@mutation disk-init-clears-the-disk-address
@check disk
@file rtl/cadr_disk_controller.sv
@note `-XINIT` clears the command register --- the 74LS175 at DCCMD 0C21 and
@note the 74LS273 at 0C10, pin 1 of each --- and stops the channel.  "The
@note disk address counters (the 74LS193s at DCDA 0A17-0A21), the command
@note list pointer (DCCLP) and the CCW latches (DCCCW) have no pin on it and
@note stand."
@note
@note The trace exercises exactly that: an `-XBUS INIT` after an error, then
@note a read of register 2 which must still hold what the program put there,
@note and then a transfer started with NO store into register 1 that walks
@note the command list it was given before.
@old
      if (xbus_init) begin
        cmd     <= 32'd0;
@new
      if (xbus_init) begin
        cmd     <= 32'd0;
        da      <= 32'd0;
@end

@mutation disk-attention-moved-to-the-multiple-unit-bit
@check disk
@file rtl/cadr_disk_controller.sv
@note `<2>` is the SELECTED unit's attention and `<4>` is "multiple units
@note selected", which `Controller` never sets and nothing in this fabric
@note can.  Moving the bit therefore loses the attention and lights a bit
@note that should be dead, and both halves of that are visible: a seek raises
@note an attention when the heads arrive, and an at-ease takes it away.
@note
@note A status bit moved is the finest grain this module has, which is the
@note same argument `disk-not-on-cylinder-at-bit-7` makes one check along ---
@note there against the no-drive constant, here against a bit only a drive
@note can raise.
@old
                            | (attention           ? 32'h0000_0004 : 32'd0))  // <2>
@new
                            | (attention           ? 32'h0000_0010 : 32'd0))  // <4>
@end

@mutation disk-reset-err-reaches-the-drive
@check disk
@file rtl/cadr_disk_controller.sv
@note `-RESET ERR` is `-LOAD CMD` OR `-XINIT` (74LS08 at DCCMD 0D14) and it
@note clears the EIGHT TRANSFER ERROR FLOPS.  The drive's own flags are not
@note among them: a fault goes away on a fault clear and a seek error only on
@note a recalibrate, and both of those act at the START and not at the store
@note into the command register.
@note
@note The trace asks the question in three rows: a write to a read-only pack
@note raises the fault, the fault-clear COMMAND is stored, the status is read
@note --- and the fault is still there --- and only then does the START take
@note it away.
@note
@note THE OTHER HALF OF THE SAME LINE IS AN EQUIVALENCE AND THE SECTION
@note HEADER SAYS SO: clearing BUSY here as well survives, because every
@note command store the trace makes while the sequencer is hung is code 0o16,
@note which clears BUSY on purpose.  This is the live mutation that tests the
@note same property, which is the rule CLAUDE.md sets --- replace an
@note equivalent mutant with the smallest magnitude that is caught.
@old
          2'd0: begin
            cmd     <= wdata;
            timeout <= 1'b0;
@new
          2'd0: begin
            cmd     <= wdata;
            timeout <= 1'b0;
            u_fault <= 8'd0;
            u_seek_err <= 8'd0;
@end

@mutation disk-seek-settle-a-tick-short
@check disk
@file rtl/cadr_disk_controller.sv
@note `seek_ns(n)` is `SEEK_SETTLE_NS + n * SEEK_NS_PER_CYLINDER`, and the
@note settle is Century Data's own figure.  Five nanoseconds off it is one
@note tick of this clock out of 1,212,055.
@note
@note It is caught because the trace walks up to the arrival rather than
@note repeating muir's arithmetic: it reads the status at
@note `grid_before(began + ns)` and again at `grid_at(began + ns)`, one tick
@note apart, and the fabric's down-counter has to reach zero between them.
@note Neither of the two seeks it does that for is a multiple of five ---
@note 6,000,000 ns and 6,060,271 --- and the counter counts NANOSECONDS by
@note five a tick for exactly that reason, so no divider decides where the
@note boundary falls.
@old
  localparam logic [27:0] SEEK_SETTLE_NS       = 28'd5_939_729;
@new
  localparam logic [27:0] SEEK_SETTLE_NS       = 28'd5_939_724;
@end

@mutation disk-index-pulse-a-tick-narrow
@check disk
@file rtl/cadr_disk_controller.sv
@note The index pulse is 4,000 ns and a sector pulse 1,240, and the block
@note counter "holds the region before through the pulse".  So the pulse's
@note WIDTH is where the counter steps, and one tick off it moves that edge
@note by 5 ns out of a region of 968,448 --- 0.0005% of the revolution.
@note
@note The trace samples either side of all eighteen trailing edges, which is
@note the only way this is visible at all, and the testbench places each of
@note those rows on an instant of its own so that the spindle is where muir
@note says it is to the tick.  Everything else about the block counter ---
@note the wrap by subtraction, the region threshold --- is checked by the
@note third sample of each region, well inside it.
@old
  localparam logic [19:0] INDEX_PULSE_NS  = 20'd4_000;
@new
  localparam logic [19:0] INDEX_PULSE_NS  = 20'd3_995;
@end
